---
run: 2026-07-07-doc-parser-calibration
task: "Can you look up the BoA one, and the 30-50 most recent bank account offer posts yourself? Would prefer it to pull ones that are either nationally available or relevant to WI residents versus ones specific to states I am not eligible. [Context: calibrate DoC import v1 parser against a real corpus; build tier picker + stale-content demotion; owner will not supply or score posts.]"
status: in-progress
created: 2026-07-07T13:30:00-05:00
config_snapshot: { planner: fable, executor: opus, worker: sonnet, codex: { planner: review-before-approval, executor: review-after, worker: review-after } }
plan_approved: true (user, after codex critique + 13 revisions)
current_step: 4b
---
## Context
Yield Vector's DoC import v1 (`parseDocPost`, shipped last night, v2026.07.08b @ `6b101bd`) was validated only against 5 synthesized fixtures. Owner reports a real failure class: tiered offers (BofA "up to $2,500" where the real bonus/hold tiers live in the post body) and stale content in living posts (values outside the newest "Update M/D/YY" section are outdated). This run: harvest a real corpus (BofA post + 30-50 recent bank-account bonus posts, nationwide-or-WI-eligible only), independently hand-label expected extractions, score the current parser, implement tier-aware parsing (preview tier picker) + date-segmentation demotion + synonym gaps found, re-score, ship. Owner explicitly time-constrained: agents do ALL harvesting and labeling.

## Key decisions
- Corpus fetch order: RSS category feed + WebFetch first (fast, server-side); claude-in-chrome via owner's browser as fallback if DoC's Cloudflare blocks WebFetch. Read-only harvesting; polite pacing; no bulk republication.
- Copyright hygiene: raw post bodies live ONLY in the session scratchpad (ephemeral). The repo gets: a corpus MANIFEST (urls, titles, dates, availability, inclusion rationale), LABEL tables (extracted facts — amounts/dates/requirements), and SYNTHESIZED fixtures replicating observed patterns. Never wholesale post text.
- Eligibility filter rules (worker-executable): INCLUDE if availability = nationwide/online-anyone, or state list includes WI, or national bank/brokerage; EXCLUDE if explicitly limited to non-WI states; ambiguous → include flagged `eligibility:'unclear'`.
- Labeling independence: labeler agents produce expected-value JSON from the posts WITHOUT seeing parser output (prevents anchoring); scoring diffs parser vs labels afterward.
- Parallelism: labelers fan out 3× on disjoint post sets (no shared files — scratchpad outputs); all repo-writing steps remain sequential.
- Target corpus: 35-50 qualifying posts incl. the specific BofA "up to $2,500 / $200k hold-tier" post (harvester must locate it even if older than the recency window).
- Label schema (fixed): {url, bank, product, availability, bonus_total|tiers:[{deposit_or_balance_min/max, bonus, hold_days?}], expiration, requirements:[{type, amount, count, deadline_days, frequency}], monthly_fee+waiver, etf+window, promo_code, dd_required/amount/count/timeframe, bonus_post_window, churn:{limit_language_verbatim, wait_months, anchor}, update_dates:[...], stale_values_present:bool}.
- Version target 2026.07.08c; per-step commit-after-review as previous runs; push at finish (owner awake/interactive).
- Codex gates per snapshot; if Codex credits still exhausted, substitute the established independent-adversarial-reviewer pattern (Opus for parser steps). (Plan gate ran successfully — credits restored.)
- PLAN-GATE REVISIONS (codex, all adopted): (1) premise corrected — an 18-real-post validation corpus exists (docs/assessments/2026-07-05/step5-corpus-validation.md); harvest step reuses/extends it rather than starting cold. (2) Step 3 defines an explicit label→parser-field NORMALIZATION layer before any precision/recall math (parser returns flat fields.{key:{value,confidence,snippet}}; labels are nested). (3) Scoring harness must handle DOMParser (jsdom-style shim; step-7's harness technique, rebuilt — scratchpad artifacts are ephemeral). (4) Date-segmentation redesigned: DoC updates are DELTAS not replacements — demote an older value ONLY on field-level conflict (a newer dated segment mentions the SAME fact); never blanket-demote undated body content (fine print/fees/churn rules are usually still current). Extends the existing per-field update reconciliation. (5) Eligibility from availability language/state list ONLY — "national institution" heuristic dropped; unclear → flagged include. (6) Tier result shape specified: `tiers: []` top-level in parse result (NOT fields); preview renders a radio tier-picker; selection maps tier → signupBonusAmount + requiredFundingAmount or maintain_balance row + daysFundsMustRemain/hold; glance "up to" value forced low-confidence + default-UNCHECKED whenever tiers detected; no tier auto-applies without selection. (7) Labeling adds 15% overlap posts (two labelers) + executor adjudication of disagreements incl. all tier tables and stale-history posts. (8) Retier: step 1 worker→executor (eligibility judgment, fetch-fallback juggling, BofA hunt); step 4 SPLIT into 4a parser-core and 4b preview-UI/apply (each separately reviewed). (9) Verbatim caps: churn limit-language ≤160 chars, snippets ≤120; corpus manifest/labels = normalized facts + URL/date provenance. (10) Step-4 gates test the parser against BOTH pasted raw HTML AND Worker-style extracted text (cloudflare htmlToText/extractArticle output form). (11) Step 5 backfills the two stale "_pending_" Commit fields in CHANGELOG (churn entry → 6b101bd; bonusflow entry → 46c0c49..c2aa49d) — docs/version preflight. (12) Success criteria (minimums, not just a table): zero high-confidence stale superseded amounts on corpus; tiered max never auto-applied; existing 5 fixtures + all testDocParser assertions still pass; expiration accuracy ≥ baseline; Worker-text path parses equivalently. (13) Per-step push this run (owner awake; repo protocol favors pushing meaningful changes).

## Steps
### 1. Corpus harvest (BofA + 30-50 recent, WI-eligible)  [tier: executor (retiered per plan gate)] [codex: review-after→vacuous, read-only] [status: done]
Intent: Collect post URLs+titles from DoC bank-account-bonuses category (RSS + category pages via WebFetch; Chrome fallback), apply eligibility rules, fetch each qualifying post's full HTML to scratchpad corpus dir, build manifest JSON. Locate the BofA tiered post explicitly. Report: counts (candidates/included/excluded/unclear), fetch method used, any blocks hit.
Expected files: scratchpad only (manifest copied into step-4 commit later)
Outcome: 63 candidates → 32 fetched full-HTML (raw DOM kept deliberately — parser targets WordPress structure incl. glance <ul> and <del>) → **31 usable** (30 include + 1 unclear[id 05 NM credit union w/ $10-donation join] + 1 body-confirmed exclude[id 11 Fidelity NE-PA]); 19-entry excluded.json audit (18 title-tag non-WI + 1 body). 10 tiered (ids 01,03,04,10,16,21,23,26,30 + excl 11). All legacy-18 re-fetched fresh + requalified (availability drift honored: BMO/Wings now "Nationwide" despite stale title tags). Zero anti-bot blocks (paced curl w/ browser UA; WebFetch markdown rejected in favor of raw HTML). **BofA = id 01: bank-of-america-400-750-business-checking-bonus, Update 7/7/26, Nationwide, non-targeted; ladder confirmed in file ($5k→$400 … $250k→$2,500). Reconciliation: owner recalled "personal/Preferred Rewards up to $2,500" — no personal BofA reaches $2,500; the $200k/tier fingerprint uniquely matches this Business Advantage post; accepted as intended target.** Coverage note: stopped at category pages 1-3 + RSS + legacy-18 → 31 within owner's stated 30-50 band; pages 4-6 available if top-up wanted (declined — marginal value low, 10 tiered posts already secured).
Files: scratchpad doc-corpus/ (manifest.json 32 rows, excluded.json, posts/01-32)
Commit: n/a (read-only step)
Flags for later steps: id 16 Huntington has PER-TIER state gating (top $600 tier excludes WI — WI gets $400 tier); id 07 age-gated under-22 (included per state-only rule).

### 2. Independent labeling, 3× parallel  [tier: worker ×3] [codex: review-after→vacuous, read-only] [status: done] (splits: A=01-10+{16,26}, B=12-21+{01,23}, C=22-32+{04,10}; 6 double-labeled posts, all tiered)
Intent: Three labelers, disjoint ~⅓ splits, produce expected-value JSON per post per the fixed label schema, from post content only (no parser access). Tiered posts labeled with full tier tables; update-dated posts labeled with current-vs-stale value distinctions.
Expected files: scratchpad label files
Outcome: 37 label files written (A:12, B:12, C:13; 6 overlap posts 01/04/10/16/23/26 double-labeled), all schema-valid JSON. Corpus confirmed MESSY — living-document contradictions pervasive: stale glance boxes never rewritten after updates (03,04,08,09,22,26,27), promo-code chains w/o restated thresholds (03), expired-banner-vs-active-update contradictions (17), availability drift incl. nationwide→county-restricted affecting WI (19 Wings!), targeted-tier exclusions (20 BMO), source-article copy-paste errors (16's "$500" orphan), stacked affiliate+bank totals convention question (27), dual targeted variants (25). Adjudication queue for step 3: the 6 overlap pairs + flagged ids 03,08,09,17,19,20,22,25,27 (esp. 19's WI-eligibility change and 09's possibly-dead offer).
Files: scratchpad doc-corpus/labels/{A,B,C}/*.json
Commit: n/a (read-only step)

### 3. Baseline scoring + miss taxonomy  [tier: executor] [codex: review-after→vacuous, read-only] [status: done] (now also owns: overlap adjudication → gold label set)
Intent: Harness extracts the REAL `parseDocPost` from index.html, runs it over all corpus posts, diffs field-by-field vs labels → accuracy matrix (per-field precision/recall, confidence calibration) + categorized systematic misses (tiers, stale-update contamination, label-synonym gaps, unit/format cases). This drives step 4's exact scope.
Expected files: scratchpad report (summary lands in this checkpoint)
Outcome: GOLD SET: 31 posts (inter-rater 79.2% pre-adjudication — 81.1% excl. the churn-anchor convention gap; worst post 16 @ 50%); 6 doubles diffed + ruled, 9 flagged adjudicated vs entry-content, 3 singles spot-checked; conventions: tiered bonus = careful-churner current max; stacked convention unified (27 → $815, overturning $715); 19 Wings = county-restricted, WI counties present → include; 20 targeted tiers excluded ✓; 25 points→null; 09 kept "likely withdrawn". HARNESS: real parseDocPost under jsdom, FIDELITY 44/44 vs app's own expectations; HTML-vs-text parity 100% (Worker path equivalence). BASELINE: field accuracy 73.4%, recall 57.8%, **26 HIGH-CONF-WRONG** (funding ×11, bonus ×6, monthly_fee ×4, expiration ×4, ddRequired ×1); **calibration INVERTED** (high 75.7% < medium 78.4%). TAXONOMY (124): STRUCTURE 46 (body facts w/o glance rows), SYNONYM 29 (**#1 bug: 'Credit card funding' glance row mis-claimed as funding requirement — 11/11 wrong at high conf** + churn-anchor heuristic ×15 + ddRequired negation), STALE 15 (10 high-conf), OTHER 15 (fee-waiver presence ×10), UNIT 12 (incl. "2026" parsed as $825 bonus), TIER-BLIND 7. baseline-report.md has ranked P0-P6 work list w/ exact phrasings. gold_max_tier_bonus + glance_headline recorded per post for tier-feature scoring.
Files: scratchpad labels/gold/* + _adjudication.md; harness/* incl. baseline-report.md, score/taxonomy/parity results JSONs
Commit: n/a (read-only step)

### 4. Implement (SPLIT per plan gate): 4a parser core → 4b preview UI  [tier: executor ×2 sequential] [codex: review-after (Codex restored) + adversarial backup] [status: 4a DONE (commit eedb027), 4b in-progress]
4b Outcome (pre-review): tier group renders when tiers.length≥2 (5 BofA tiers + No-tier, none pre-selected); pure `_docEffectiveFields(result, sel)` single-source resolver feeds both render + apply; selection live-updates + force-checks affected rows (deposit→requiredFundingAmount; balance→maintain_balance user row; dd_total/spend→user rows); chosen bonus → HIGH forced-checked; provenance note on Apply; selection resets on re-parse/Clear; radio rides existing form change delegation (no listener dup). Gates: node --check ✓, fidelity 63/63 ✓, BofA E2E ($15k→750/15,000, switch, revert, Apply+Save+derived row) ✓, balance-kind synthetic ✓, XSS escaped ✓, 380px ✓, non-tiered pastes byte-identical panel ✓.
4b Codex review: [P1] balance tiers don't drive requiredFundingAmount (capital model underestimates); [P2] dd_total tiers write only a user row — invisible to legacy DD completeness/cash-flow → offer stays draft; [P2] tier switch re-render clobbers user checkbox choices (capture/restore by data-doc-key). + planner per-kind funding-row disposition (balance→funding=threshold; spend/dd_total→uncheck funding row; deposit→unchanged). All dispatched to 4b implementer (a4c402a4949481505).
4a RESUME NOTE: first 4a agent was killed MID-FLIGHT by user interrupt (not pre-launch as planner initially believed — its WIP edits + fixtures 06/07 were already in the working tree and were the token burn the user asked about). Relaunched agent VERIFIED the WIP empirically against the baseline rather than trusting it, fixed 3 blocking bugs (testDocParser meta-key assertions _tiers/_signupBonusConfidence; DOC_TEST_EXPECT['06'] artifact expression; cc_funding_note unmapped→DOC_FIELD_MAP notes-only row; tier objects completed w/ hold/deadline nulls), extended fixtures README.
4a Outcome — ALL GATES PASS, before→after: accuracy 73.4→85.4% (≥85 ✓); recall 57.8→71.1% (≥70 ✓); high-conf-wrong 26→2; stale-high-conf 10→0 ✓; CC-funding mis-claims 11→0 ✓ (docIsCardFundingLabel guard + funding requires deposit/opening companion token); calibration high 96.6% > med 87.2% ✓ (was inverted); expiration 16/23→20/23 ✓; tiers[] emitted on 10 posts, BofA ladder complete + headline forced low/tiered flag ✓ (docScanTiers bullet grammar + monotonic per-kind ladder filter); delta-aware demotion via docDateSegments + docReconcileScalar (newest-dated-segment-wins per FIELD, undated fine print keeps confidence) ✓; churn anchor keyed-phrase detection replaces positional heuristic; year-in-date guard kills "2026→$825" class; parity 55/55 ✓; fidelity 44/44→63/63 (+19 from fixtures 06/07, deliberate) ✓; preview smoke: non-tiered renders/applies, tiered renders w/ tiers inert (no leak) ✓. 2 residual high-conf-wrongs ACCEPTED w/ rationale ([05] unconditional glance "None" vs body $5 fee; [10] stale glance expiration vs body app-window — naive body-date promotion would break posts 06/28): over-fit guard per plan. Recall long-tail deferred (funding ×7, waiver ×11, post-window ×5, hold ×6, churn-anchor ×10) — non-blocking.
Codex post-review (REAL codex, credits restored): [P1] "lowered to $250 (was $350)" allegedly keeps stale max (:10067 Math.max extractor) — CONTRADICTS passing fixture 07, needs empirical adjudication; [P2] "Direct deposit required: Not required" allegedly affirms (affirm-before-negate, :10137); [P2] docDateSegments allegedly swallows glance/body into oldest dated segment (:9690). All 3 dispatched to 4a implementer (a21e404f4be315b42) with REPRODUCE-or-REFUTE-with-evidence instructions incl. adversarial same-segment lowered-bonus variant + BofA segment-assignment dump + full corpus re-score on any segmentation change.
ADJUDICATION: ALL 3 REPRODUCED + FIXED (Codex right on all counts; fixture 07 had passed via a different path — cross-segment reconcile — masking the single-segment P1 case). Fixes: reduction-verb target wins over Math.max in BOTH extractors (increase-verb guard preserves [23] range case — mid-fix regression caught+corrected); DD negation-first w/ in-negation token exclusion (17/17 matrix); glance heading = segment boundary resuming undated base (BofA glance dump 2→67 lines in base; pins reframed to corpus reality: glance stale/updates fresh). NET: accuracy 85.39→84.93 (−1 cell) — [06]/[16] had been correct BY ACCIDENT of the P2b bug; [14] newly correct; recovering them = revert-the-bug or over-fit. PLANNER DECISION: 84.9% ACCEPTED vs ≥85 gate (gate purpose = genuine improvement; correctness > vanity threshold; all dangerous metrics held: high-conf-wrong 2, stale 0, CC 0, calibration 96.7>87.1, fidelity 63/63, in-app pins 12/12, parity 55/55). testDocParserRegressions() (12 pins) shipped in-app.
Intent: (a) Tier scanner: detect tier tables/bullet patterns in the CURRENT-dated body; preview gains a "Select your tier" group when >1 tier found — choosing a tier sets bonus + funding/maintain-balance requirement from THAT tier; glance "up to" value demoted to low confidence when tiers detected. (b) Date segmentation: content under newest "Update M/D/YY" marker = current; conflicting matches from older/undated segments demoted to low + "older iteration?" flag. (c) Synonym/pattern fixes per step-3 taxonomy. (d) New synthesized fixtures (BofA-style tiered + update-heavy) + testDocParser expectations + fixtures README. Gates: node --check, harness, in-app E2E on the tiered fixture, feed untouched, 380px.
Expected files: index.html, docs/fixtures/doc-samples/*
Outcome:
Files:
Commit:

### 5. Re-score + docs + ship  [tier: executor] [codex: review-after→light review] [status: pending]
Intent: Re-run step-3 harness on the SAME corpus → before/after accuracy table; commit corpus manifest + label tables to `docs/fixtures/doc-corpus/` (facts only); HANDOFF R68 + CHANGELOG + APP_VERSION 2026.07.08c. Planner reviews, commits, pushes.
Expected files: index.html (version), docs/fixtures/doc-corpus/*, HANDOFF.md, CHANGELOG.md
Outcome:
Files:
Commit:

### 6. Synthesis  [tier: planner] [status: pending]
Intent: Report to owner: corpus composition, before/after accuracy, what specifically improved (BofA case demonstrated), residual gaps + whether v2-LLM assist is still worth deploying for the remainder.
Expected files: none (chat)
Outcome:
Files:
Commit:
