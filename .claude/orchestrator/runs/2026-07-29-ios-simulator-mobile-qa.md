---
run: 2026-07-29-ios-simulator-mobile-qa
task: "when finished with the previous tasks, utilize the iOS Simulator to ensure that Yield Vector functions 100% appropriately, and presents 100% appropriately on mobile in addition to desktop. Ensure no odd formatting, asymmetrical spacing, excessive or inadequate sizing of any component causing cut off text, excessive white space infringing upon something else/adjacent, etc."
status: done
created: 2026-07-29T06:55:00Z
config_snapshot: { planner: fable, executor: opus, worker: sonnet, codex: { planner: review-before-approval, executor: review-after, worker: review-after } }
plan_approved: true  # owner advance directive (in-chat 2026-07-29): elect top plan following Codex review; Codex findings folded below
current_step: 5 (complete)
---
## Context
Sequel run queued behind 2026-07-29-scope-arch-aesthetic-review (dispatch only after that run reaches done). Yield Vector v2026.07.14b on main (HEAD 63facc2). Goal: real-device-class QA — the app driven in the iOS Simulator (Safari, real WebKit + safe-areas + touch) AND desktop, hunting layout/formatting defects (cut-off text, asymmetric spacing, over/under-sized components, whitespace collisions) and functional breaks in core flows. Defects found get fixed, re-verified, and released per repo protocol. Owner advance-approval (in-chat 2026-07-29) covers electing the top plan after each Codex review — no further approval gates this run except a genuine scope fork.

## Key decisions
- Step-4 scope addition (planner, under the owner's advance approval): the step-3-discovered 4px .btn-icon hit-band overlap is fixed in the release step with an exact spec (same defect class as the folded checkbox finding; judgment already done in step 3, spec-complete => worker-appropriate).
- BASELINE REVALIDATED at dispatch (2026-07-29, per G8): HEAD still 63facc2, v2026.07.14b, live==HEAD confirmed by run-1 step 2; worktree carries the pre-existing modified 2026-07-13 run checkpoint + run-1/run-2 checkpoint files (committed by planner at run-1 close) — no product-file changes. Run 1 reached step 5 (email) at dispatch time; QA sweep is read-only so no conflict.
- PLAN GATE (Codex Sol, 2026-07-29) — findings FOLDED: (G1) owner-data safety hardened: unique fresh port, clear storage/caches/service-workers first, assert `yv-allow-local-sync` absent + sync unconfigured, block/monitor api.github.com + any Worker URL (localhost guard alone doesn't cover the DoC Worker, sync-pwa.js:235). (G2) Chromium-380px fallback CANNOT close an iOS gate — if no simulator boots, R92 stays open and the report says so. (G3) results scoped to Safari-in-simulator explicitly; standalone-PWA safe-area checked only if installable there, else stated as untested. (G4) breakpoint matrix added: automated Playwright checks bracketing the CSS boundaries 340/420/480/720/880/1024 (index.html:270) + desktop 1024 AND 1280; simulator covers one small + one notched phone if available, portrait AND landscape, keyboard-open modal state. (G5) "zero console errors" is unmeasurable via the simulator MCP — desktop console via Playwright, simulator via the in-app diagnostics ring, limitation reported honestly. (G6) surface sweep replaced with a concrete checklist: 4 nav views + 2 Plan segments; "Upcoming actions" on Home; checklists live on offer cards (render-shell-overview.js:88); include optimizer apply/undo, persistence-after-reload, chart tap targets, validation failures, commitment/event tables, offline/SW-reload — anything skipped is listed as an exclusion. (G7) R92 gate items: only (b) hero-label and (e) timeline-label-fit are objectively verifiable; (a)(c)(f) are owner eye-calls — screenshots are EVIDENCE, closure stays with the owner. (G8) baseline (HEAD/version/git status/HANDOFF) re-validated at dispatch time — run 1 may move it. (G9) full battery runs EVEN IF zero defects found (else "functions appropriately" rests on manual sampling alone). (G10) synthetic fixture made reproducible: schema, scenario→offer mapping, dates, expected states, reset procedure recorded in the QA report. (G11) QA artifacts live in the SESSION SCRATCHPAD (/private/tmp/..., outside the checkout) — Codex's untracked-files concern is moot but the absolute path is now explicit. (G12) fixes split by subsystem with per-subsystem commits (30-min push protocol respected). (G13) post-review fold → RERUN affected UI cases + pins + node --check + stray-scan BEFORE push. (G14) release protocol completed: CHANGELOG.md entry required (AGENTS.md:67); target version 2026.07.29 if fixes ship; verify all 21 literals + 0 strays; verify LIVE Pages serves the new version post-push; tag naming follows AGENTS.md convention stable-YYYY-MM-DD (so stable-2026-07-29 for a new release) — run 1's "stable-2026-07-14b" suggestion is dropped as convention-breaking; report distinguishes "no new release" from "v2026.07.14b now eligible for an owner-approved stable tag". (G15) tiers: step 3 split — executor re-verifies + folds review findings; NEW worker step handles the mechanical release (bump ×21, CHANGELOG, HANDOFF, commit/push) after final verification, matching the prior release's separation.
- OWNER-DATA SAFETY (hard rule, same as run 1): the simulator NEVER opens the live site with sync configured; QA runs against a locally served fresh origin (simulator Safari shares the host loopback, so http://localhost:<port> works) seeded with SYNTHETIC data via a same-origin seed page (write localStorage → redirect into the app). The app's `yv-allow-local-sync` guard already blocks sync on localhost by default. Zero Gist/token exposure.
- iOS Simulator is the primary mobile surface (real WebKit rendering, notch/safe-area, touch targets) — Playwright 380px emulation is the FALLBACK only if no simulator can boot on this Mac (record the substitution honestly if so).
- The simulator MCP is a single shared resource → exactly ONE agent drives it at a time. Desktop verification uses Playwright headless in the same agent (no Browser-pane contention with the main session).
- This QA doubles as evidence capture for the outstanding owner device-check gate (R92 items a,b,c,e,f) — each item gets a screenshot + pass/attention note so the owner can close the gate from the report and the stable tag can finally be cut (recommended name per run 1: stable-2026-07-14b; cutting it stays an owner call).
- Fix scope guard: layout/formatting/functional DEFECTS only. No palette/theme changes (run 1's aesthetic variants are a separate owner decision); locked design values in AGENTS.md respected — a defect whose fix would touch a locked value gets flagged, not fixed. Typography stays parked.
- If the sweep finds ZERO defects: no version bump, no release — the report says so plainly and the run closes.
- Release protocol if fixes ship: APP_VERSION bump ×21 (js/runtime-status.js, sw.js, index.html ×19 `?v=`), full battery (optimizer 86, feasibility 21, parser 24, fidelity 67, dd-matrix, p2b, node --check), HANDOFF.md entry prepended, Codex adversarial post-review pre-push, explicit-path git add only (owner-owned paths untouched), push to main.

## Steps
### 1. Simulator + desktop QA sweep  [tier: executor] [codex: review-after] [status: done]
Intent: Boot an iPhone simulator (attach the live panel first — cheap, and the owner may watch), serve the repo on a local port with a synthetic-data seed page, and sweep EVERY surface at mobile: Home (hero chart, stat row, banners), offer cards (all statuses incl. stale-chip + windowPassed states), timeline (narrow bars with $ labels, phone width), Optimize panel (pre/post-run, breach banner, alternative cards), Add/Edit offer modal (all offer types, requirement rows, date pickers incl. grayed days + violation hints, either/or chooser), Add/Edit event modal (neutral picker, recurrence), Settings, reminders/checklist views, DoC import (paste mode). Functional passes on simulator: add offer → appears everywhere; edit dates via picker; run optimizer; Move-to-today tap; modal open/close; scroll behaviors. Same sweep on desktop via Playwright 1280px. Hunt specifically: cut-off/clipped text, asymmetric spacing, over/under-sized components, whitespace collisions, safe-area intrusions (notch/home-indicator), tap-target adequacy, horizontal overflow, console errors. Capture R92 gate items (a) neutral-picker formatting vs colored pickers, (b) hero-chart right end at a Dec-11-horizon scenario, (c) chooser copy order, (e) timeline $-label fit at phone width, (f) DoC import placement — one screenshot each + note. OUTPUT: severity-ranked findings list (P1 functional / P2 clear visual defect / P3 polish) each with screenshot path + suspected cause (file:line where cheap), plus the gate-item evidence pack. Screenshots to scratchpad/qa/. Read-only step — no fixes yet.
Expected files: scratchpad/qa/*.png, scratchpad/qa/findings.md (no repo changes)
Outcome: Full QA sweep on real iOS WebKit (iPhone 17 Pro simulator, iOS 26.5, 402x874pt) + Playwright desktop against a frozen synthetic fixture (10 offers/5 events, anchor 2026-07-29, horizon forced 2026-12-11) on fresh port 8931. RESULT: 0 P1 functional breaks (add/edit/persist/optimizer/Move-to-today/reload all correct; diagnostics ring clean; desktop console 0 errors). 3 P2 visual defects: P2-1 hero-chart Y-axis labels clipped — 36px column vs 44.7px labels, minus sign and $ cut off so +$200K and -$200K render identically (index.html:587-588; BOTH platforms; most severe — misrepresents sign on financial data); P2-2 header overflows viewport at 721-804px (compact-header breakpoint 720 vs 805px intrinsic minimum; hits iPad mini portrait 744pt); P2-3 offers grid overflows at 481-518px (single-column breakpoint 480 vs 519px two-column minimum). 13 P3 polish items incl. tap targets far below 44pt (bottom nav 34pt; toggles 18-24px), "1 drafts" plural, Settings native date input inconsistency, always-ellipsised timeline axis label, no inline warning on recurrence-end-before-start, FAB overlaying text, no top/left/right safe-area handling, iOS keyboard covering the add-offer modal on autofocus, x4 duplicated violation hint, always-shown "unavailable" legend. R92 GATE: (b) hero right end at Dec-11 horizon PASS objective (no duplicate month label); (e) timeline $-label fit PASS objective ($500K fits a 97.8px bar, zero clipping); (a)(c)(f) evidence captured for owner eye-calls. Owner-data safety attested: external-request counter 0 across all 10 runs, sync-config + allow-local-sync asserted absent both platforms, synthetic data only (owner strings scrubbed + regex-asserted). Exclusions recorded: simulator rotation (System Events perm denied), optimizer apply/undo (fixture infeasible by design), standalone PWA (not installable in sim), simulator JS console (MCP can't read; diagnostics ring substituted), DoC paste round-trip, history/recovery UI (gated on cloud sync), small-phone device (brackets covered on desktop).
Files: scratchpad/qa/ — fixture.md, fixture.json, enrich.js, serve/qa-seed.html, 10 harness scripts, 7 JSON reports, shots/ (40 PNGs). Repo untouched.
Decisions: frozen fixture replayed via same-origin seed page so both platforms load byte-identical data; desktop Playwright for exact measurement, simulator for real-WebKit/keyboard/safe-area; Y-axis defect kept P2 per taxonomy but flagged as P1/P2 boundary; chart-tooltip overflow excluded as measured false positive (opacity:0, parked).
Open issues: real-device landscape untested (simulator rotation needs accessibility perm — AppleEvent -1712); optimizer apply/undo needs a capital-feasible fixture variant; recurrence-end save-time validation inconclusive (save blocked by unrelated empty fields); one mis-aimed tap typed "BMO" into Safari's own URL bar (Safari chrome, no app/owner data; in-page counter stayed 0).

### 2. Fix batch  [tier: executor] [codex: review-after] [status: done]
Intent: Fix confirmed P1/P2 findings (P3 only where trivial and riskless), respecting the scope guard in Key decisions. CSS/layout in index.html + render modules; behavioral fixes get pins. If any finding's fix would touch a locked design value or is a taste call, flag it in the report instead of fixing. APP_VERSION bump ×21 + HANDOFF entry IF anything shipped. Skipped entirely if step 1 finds nothing actionable.
Expected files: index.html, js/render-main-views.js and/or other touched modules, HANDOFF.md
Outcome: All 3 P2s + 10 of 11 candidate P3s fixed across 5 commits, verified by render probes on the frozen fixture. P2-1: Y-axis column now content-aware (--yv-yaxis-w measured from widest rendered label, 36px floor / 88px ceiling) — max clip 0.0px at 375/402/744/1024 (was 14.7px). P2-2: compact-header breakpoint moved to 804/805 across four queries (+ the :613 portrait chart-svg rule) — scrollWidth==clientWidth at all 14 probe widths incl. the whole former band. P2-3: content fix + breakpoint — the .ddm-pill chain was unshrinkable (missing min-width:0); card min-content dropped ~270->~195px, two-column band set at 560px with minmax(0,1fr) (560 over measured 519 because the floor is data-dependent). P3s: plural guard; timeline axis label wraps to 2 lines inside the existing 32px row (widening would shift every row); recurrence end-before-start inline warning + save-time guard; violation hint deduped by message text (6 hints -> 2 in repro); legend "unavailable" only when the rendered month has blocked days; FAB scroll clearance; touch-autofocus dropped via shared focusFirstFieldUnlessTouch() (extended to event + commitment modals; doc-import keeps autofocus by purpose); commitments heading CSS; hit areas >=44pt via (pointer:coarse)-scoped absolutely-positioned ::before with visual sizes unchanged (tap 15px outside a checkbox toggles it); top/left/right safe-area env() on fixed/sticky chrome (+ repaired the <=804 .app-header rule that was discarding insets); Settings projection-start converted to yv-date neutral (Clear now means "today" per the field's auto-advance semantics; unreadable typed input keeps stored value). Batteries: node --check clean x4 modules, feasibility 21/21, optimizer 86/86, zero page errors, zero external requests. No locked design value touched. Nothing pushed; no version/CHANGELOG/HANDOFF edits.
Files: index.html, js/render-main-views.js, js/modals-forms.js, js/events-actions-data.js, js/dd-widgets.js
Commit: bce7dfa (P2-1) · cd9562a (P2-2/P2-3/P3-8/P3-11) · c98a643 (P3-1/P3-9) · 783a192 (P3-6 + header-inset repair) · bd4dccf (P3-2/3/7/10/12/13)
Decisions: content-aware axis over a new magic number; 560px offers band over measured 519 (data-dependent floor); header pinned 1px under its fixed 805px minimum (in-band shrink skipped as unmeasured visual change); hint dedupe split into pure dateViolationMessage() + renderDateViolationHint().
Open issues (owner-visible flags for the final report): 721-804px (incl. iPad mini portrait 744pt) now renders the PHONE layout — visible device-class shift; 519-559px now stacks single-column; commitment modal still has 2 native date inputs (#c-start/#c-end, out of scope — Settings was not the only native one, premise slightly off); zero slack at 805px (any nav/button copy growth reopens the band 1px higher); safe-area verified structurally only (env()=0 headless) — simulator re-check owed next step; FLAG-ONLY items P3-4 (events-table responsive design), P3-5 (title ellipsis plausibly intentional), bottom-nav 44pt height (owner option).

### 3. Re-verify + fold review findings  [tier: executor] [codex: review-after] [status: done]
Intent: Re-drive every fixed screen on simulator + desktop (before/after screenshots at the affected breakpoints), run the FULL battery + node --check (this step runs even if step 2 shipped nothing — battery-only in that case), confirm desktop console clean via Playwright + simulator diagnostics ring clean, zero horizontal overflow at 340/380/simulator widths. Then Codex adversarial post-review (scope branch, -m gpt-5.6-sol); fold critical/high findings and RERUN affected cases + pins + stray-scan after folding.
Expected files: repo (fix-up edits only if review findings folded)
Outcome: Desktop: 84/84 overflow cells clean (21 widths x 4 views), Y-axis 0.0px clip at all 9 widths (column steady 53px, no ratchet across 3 re-renders), all step-2 behaviors re-exercised clean, 0 console/page errors. Simulator (iPhone 17 Pro): all fixed surfaces re-shot with before/after pairs — Y-axis signs fully rendered, no keyboard auto-open, 22pt checkbox toggles from 10pt outside, logo clear of the Dynamic Island at 79pt (portrait header scrolls away by pre-existing design), FAB clearance + commitments heading good, diagnostics ring clean. iPad mini 744pt portrait captured: phone layout renders cleanly (render-only; panel/input permission declined for that device). FULL battery: optimizer 86/86, feasibility 21/21, fidelity 67/67, parser 24/24, dd-matrix PASS, p2b PASS, node --check 20/20 + sw.js. External requests 0 both platforms. CODEX ADVERSARIAL (gpt-5.6-sol): verdict FAIL, 0 critical / 2 high; 3 findings independently reproduced and FOLDED in a4c7553 — (H) the new safe-area side gutters pushed the desktop header's 805px minimum to 869px, overflowing every notched-iPhone landscape width (introduced by this diff; side gutters restored flat, top inset kept); (M) 44pt checkbox hit-bands overlapped 38px-spaced checkbox rows — a tap under "Include in projection" toggled the neighbor; expansion removed for .checkbox-row (the label row is already full-width); (M) unreadable Settings date input left stale text on screen — now restores stored display. Rejected with verified reasons: APP_VERSION finding (= step 4, out of scope), hint-dedupe (deliberate step-2 design, advisory-only), landscape mobile-nav height (pre-existing, not in diff), chart-svg 720 leftover (iPad screenshot proves flex-fill renders BETTER than Codex's fix), 88px clamp (needs ~$1e18).
Files: index.html, js/events-actions-data.js
Commit: a4c7553 (three Codex folds)
Decisions: header fold via flat side gutters, NOT Codex's breakpoint-to-880 (would push 805-879px desktop windows to phone layout — larger change than the polish it buys); checkbox fold via removing expansion, NOT min-height 44px (visible row growth vs step-2's no-visual-change commitment); first two tap-tests invalidated (disabled checkbox on a confirmed offer) and re-run on an enabled card.
Open issues: NEW 4px hit-band overlap between adjacent .btn-icon/.btn-icon.btn-danger pairs (5 pairs; tap in the edit/delete sliver now hits delete; confirm()-guarded) — absorbed into step 4 with exact spec; landscape cutout insets no longer reach the 805-1024 desktop header band (deliberate, cosmetic); the (max-width:720px) portrait chart-svg rule is an undocumented leftover kept on merit; landscape mobile-nav height discards bottom inset (pre-existing); simulator landscape untested (rotation permission denied) — header fold verified by probe only; Settings picker reload behavior confirmed as rollProjectionStartIfStale by design.

### 4. Mechanical release  [tier: worker] [codex: review-after] [status: done]
Intent: Only if steps 2-3 shipped changes: APP_VERSION → 2026.07.29 across all 21 literals (js/runtime-status.js, sw.js, index.html ×19 `?v=`), 0-stray scan, CHANGELOG.md entry per AGENTS.md:67, HANDOFF.md entry prepended, explicit-path git add, commit + push to main, then verify the LIVE Pages site serves 2026.07.29 (curl, allow 30-90s rebuild + 600s edge TTL). Skipped with a stated reason if nothing shipped.
Expected files: js/runtime-status.js, sw.js, index.html, CHANGELOG.md, HANDOFF.md
Outcome: All 7 sub-steps complete. Micro-fix: .btn-icon::before horizontal expansion dropped (left:0;right:0, own width; vertical 44px kept) — geometry-agnostic vs half-gap tuning; verified with a NEW geometry-aware probe reading real computed ::before boxes (the existing probe_folds.py hardcodes a centered-44 assumption and could not have detected the change): 0 overlaps across all views + modals, sliver-click resolves to neither button, 0 external requests. APP_VERSION 2026.07.29 x3 places; stray-scan 0 hits of 14b / 19 hits of ?v=2026.07.29. CHANGELOG + HANDOFF R93 prepended (Current state header advanced to Round 93). Re-verified: node --check 19 modules + sw.js clean, optimizer 86/86, feasibility 21/21, Playwright smoke (boots, version visible, 0 console errors, 0 external). Pushed fd37c2c (4897421..fd37c2c); live verified in ~60-90s: all three endpoints serve 2026.07.29, 0 strays.
Files: index.html, js/runtime-status.js, sw.js, CHANGELOG.md, HANDOFF.md (+ session-scratchpad probes, not committed)
Commit: fd37c2c, pushed origin/main
Decisions: selector-level fix verified by live sweep rather than hand-enumerating the 5 known pairs; new probe built because the old helper would have reported a false result either way; checkpoint files left unstaged per owner-path restriction (planner commits them at run close).
Open issues: none — all spec-mandated checks passed.

### 5. Run report  [tier: planner] [codex: review-after] [status: done]
Intent: Compact owner summary — findings table (fixed / flagged / gate-item evidence), before/after shots, battery results, release version if any, and the device-check gate disposition (objective items (b)(e) verified or not; eye-call items (a)(c)(f) presented as evidence for the owner). Because this report may declare gate/release readiness, it gets a lightweight Codex look before delivery.
Expected files: none
Outcome: Final owner report written (scratchpad/qa-run-report.md) and Codex-fact-checked. INCIDENT: the first fact-check invocation (2026-07-29 04:03) hung as a background zombie for ~60h — the codex process died silently (machine sleep) with a 0-byte output file and no exit event, so the task chip stayed "Running" until the owner noticed on 07-31; task killed, check re-run clean in minutes. Re-run verdict: 5 findings, ALL FOLDED — (H) "zero breaks"/"0 console errors" scoped to what was actually exercised with explicit exclusions; (H) ">=44pt hit-areas" corrected (worst offenders expanded toward the guideline; 34pt bottom nav deferred; checkbox rows rely on the full-width label row); (M) phone-pickup claim softened to network-first-SW semantics; (M) "only open items" corrected to include eye-calls + deferred UI decisions + untested surfaces; (L) polish count 11->12. Report delivered in chat with gate-evidence + before/after screenshots.
Files: scratchpad/qa-run-report.md
Decisions: re-ran the fact-check rather than skipping it after the zombie — the machine was awake and the two prior hangs were sleep casualties, not tool failures; verdict justified the re-run (2 high-severity overclaims caught in my own draft).
Open issues: background codex exec calls do not survive Mac sleep and leave stuck "Running" chips — future long Codex calls in this environment should run foreground with a generous timeout or be checked for a 0-byte output file before being trusted.
