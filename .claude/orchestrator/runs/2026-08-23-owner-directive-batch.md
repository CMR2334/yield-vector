---
run: 2026-08-23-owner-directive-batch
task: "Implement the owner-approved 2026-08-23 directives: (1) date-input picker-primary tap behavior (keypad only on 2nd tap); (2) DoC import promoted + merged into one 'DoC URL' field; (3) entity/email auto-defaults by business-vs-personal detection (Settings-stored values, public repo carries no personal values); (4) chooser path order flipped hold-first (owner: 'hold first'). Owner: 'Looks good to do so if not yet done.'"
status: done
created: 2026-08-23T16:20:00Z
config_snapshot: { planner: fable/max, executor: opus/high, worker: "codex:gpt-5.6-luna"/max (fallback haiku), review: gpt-5.6-sol/high, codex: { planner: review-before-approval, executor: review-before-approval, worker: review-after } }  # re-read 2026-08-23; supersedes the stale in-plan snapshot per Codex F3
plan_approved: true  # standing owner directive (elect top plan post-Codex-review) + explicit "Looks good to do so"; all 10 gate findings folded below; the plan critique doubles as step-1 pre-gate (executor mode is now review-before-approval)
current_step: done
---
## Context
Yield Vector, main @ c86e03f, v2026.07.29 live (verified). Specs live in docs/BACKLOG.md "Owner directives — 2026-08-23" (committed this morning); this run implements them. Owner decisions also closed this morning: eye-call (a) event calendar STAYS neutral (no code — record only), (c) chooser order flips hold-first (code, this run). Standing owner directive (2026-07-29, in-chat): elect the top plan/suggestion following Codex review — no approval gates beyond the plan critique. Stable-2026-07-29 tag still awaits the owner's explicit word — NOT cut by this run.

## Key decisions
- MID-RUN OWNER DIRECTIVES (2026-08-23, in-chat): (1) "cut the tag" — stable-2026-07-29 CUT at fd37c2c and pushed (done by planner, closes the R92-era gate). (2) Entity/email values must be stored privately without breaking app or repo — SCOPE AMENDMENT sent to the step-1 agent mid-flight: catalogs move to synced app state (private Gist), source keeps only generic placeholders, migration rebuilds the user's catalog from the union of values in his own existing offers (no personal string or matching-regex in source), seed data de-personalized, the auto-default mapping becomes two Settings pickers (empty in source; owner sets once, syncs everywhere), acceptance check = a PII grep over js/ + index.html + sw.js returning zero hits (release step re-runs it). NO git-history rewrite (owner constraint: don't mess up the repo) — history remains a separate decision.
- PLAN GATE (Codex Sol, 2026-08-23, verdict revise-before-execution — ALL FOLDED): (F1-CRITICAL, verified) the privacy invariant was ALREADY broken at HEAD — migrations-catalogs.js:246-254 hard-codes the owner's full entity/email catalogs (incl. ethirsystems@gmail.com) and seedSampleData carries his entity string; REVISED APPROACH: feature 3 uses the EXISTING hard-coded catalogs per current app architecture (values are public today by the owner's own established practice); a source scrub + git-history rewrite is recorded as a SEPARATE OWNER DECISION (destructive, not this run); add a PII-scan note to release checklist; checkpoints stay value-free. (F2) defaulting is a tri-state state machine: entityKind unknown|personal|business, classified ONCE after manual entry settles / import completes (not field-sequentially), auto-filled values tracked separately from user-touched (auto may replace auto or empty, never user input), pins incl. the false-positive case (personal offer whose fee-waiver text mentions business accounts). (F3) routing snapshot refreshed (see frontmatter) — worker steps execute on Codex Luna via the codex runtime per the owner's newer /tiers config (supersedes 2026-07-29 Codex-never-executes); R91 hang caveat stands: on a workspace-write hang/approval stall, fall back to the haiku Claude worker and record it. (F4) step 2's embedded adversarial IS its review (tag corrected, no duplicate); step 3 stays worker-mechanical but final live/SW-convergence verification moves to step 4 (planner). (F5) step-1 expected files += js/app-state.js (settings defaults), js/render-main-views.js (Settings UI), js/migrations-catalogs.js (catalogs). (F6) date-input spec is a full state machine: arm-before-focus; first tap = picker only; second tap on the ACTIVE field closes the picker THEN raises the keypad; state resets on blur, selection, field switch, picker/modal close, and invalid input; the global .yv-date open handler (events-actions-data.js:25) is scoped/tested explicitly incl. the Settings field; simulator tests run with the hardware keyboard DISABLED. (F7) DoC URL merge test matrix: one-field save/edit, mocked Worker success/failure, no-Worker-configured, existing docUrl values preserved on migration, churn re-check; July deploy history is not current evidence — paste mode must work regardless of Worker state. (F8) the full battery + DOM smoke + import-map/precache consistency re-run AGAINST THE FINAL RELEASE COMMIT (post-bump, pre-push) in step 3 — the pre-bump battery does not count. (F9) HANDOFF R95 records hold-first as decided+shipped and marks the shipped directives in BACKLOG; the P1 mobile-QA harness is explicitly DEFERRED to the next session (this run is large enough). (F10) exact-path staging only; the finalized run checkpoint is committed at close; stable-2026-07-29, if the owner approves it, targets fd37c2c (the release commit), not HEAD.
- One executor implementation batch (items 1–4 together): all four touch the offer/event form surface (js/modals-forms.js, js/dd-widgets.js, js/doc-import-templates.js, index.html) — concurrent agents would collide; the hold-first flip is a one-liner not worth a separate worker step.
- PRIVACY (hard): the public repo must never carry the owner's entity names or emails. The entity-defaults feature ships as a Settings-backed map (synced app state) with EMPTY defaults + Settings UI; the owner pastes his values once post-release (they're in the assistant's private memory, not here).
- Detection rule for business-vs-personal: conservative — prefill business defaults only on a clear business signal (offer/bank/DoC-post text or type); otherwise personal defaults; NEVER overwrite a user-entered value; always overridable (owner sometimes uses his sole prop).
- Date-input behavior: first tap opens ONLY the yv-date picker (no iOS keypad — e.g. inputmode="none" until armed); a second tap on the active field switches to manual typed entry; typed-violation hints and picker constraints (R90) must be preserved intact. Applies to offer AND event forms. Desktop typing ergonomics must not regress (pointer: fine keeps current behavior if that's cleaner).
- DoC URL merge: promote import out of the collapsed Advanced disclosure to the modal surface; ONE field labeled "DoC URL" merged with the existing source-URL bubble (its stored value/semantics preserved for existing offers); import operates from it (Worker fetch when configured — the Worker IS deployed per HANDOFF 2026-07-08 history; pasted post text stays supported); remove the redundant second input.
- Release: v2026.08.23 per protocol (bump ×21, CHANGELOG, HANDOFF R95, full battery, live verify). Worker-tier mechanical step after executor verification, mirroring run 2026-07-29-ios-simulator-mobile-qa's G15 split.

## Steps
### 1. Implement directives 1–4  [tier: executor] [codex: pre-gated-by-plan-critique] [status: done]
Intent: Build all four features per Key decisions + BACKLOG specs; add/extend pins for behavioral changes (chooser order, defaults prefill, import path); node --check + bare-node batteries before handoff; per-subsystem commits, no push, no version bump.
Expected files: js/dd-widgets.js, js/modals-forms.js, js/doc-import-templates.js, js/doc-parser.js (if detection touches parse), js/offer-model.js (choosablePaths order), js/events-actions-data.js, js/app-state.js, js/render-main-views.js, js/migrations-catalogs.js, index.html
Outcome: All four directives built + mid-run F3 amendment honored. F1: pure dateTapDecision() (date-format-core.js:334) + DateFieldTap DOM half (dd-widgets.js:236); arming = readOnly + inputmode=none on pointerdown capture (readOnly is what actually suppresses the iOS keypad); applied to EVERY .yv-date incl. Settings (one rule everywhere); desktop untouched via pointer:coarse gate. F2: #f-doc (name=docUrl) IS the import field; #doc-fetch-url + copy-indirection deleted; status/preview visible while the paste disclosure is collapsed; churn copy now "Fetch & Parse". F3 (amended): catalogs -> settings.entityOptions/emailOptions rebuilt by migrateEntityCatalogs from the union of the user's own offers (idempotent; fresh device = generic placeholders); defaults -> settings.entityDefaults EMPTY in source (ships inert until owner sets the two Settings pairs); classification uses identifying text only (names/title/URL slug — fine print can only yield personal); saving an offer joins its values to the catalogs; Settings gained an "Entities & emails" editor + 4 pickers; sample data synthetic; PII grep over js/ + index.html + sw.js = ZERO hits (release re-runs it). F4: hold-first order flipped + composed copy verified. Batteries: optimizer pins 86->106 + NEW testEntityCatalogPins 5/5, feasibility 21/21, parser 24/24, 24-check jsdom DOM smoke (scratchpad, not committed — P1 harness deferred per F9), node --check 20 modules + sw.js clean.
Files: js/offer-model.js, js/optimizer-engine.js, js/date-format-core.js, js/dd-widgets.js, js/events-actions-data.js, js/modals-forms.js, js/doc-import-templates.js, js/migrations-catalogs.js, js/app-state.js, js/render-main-views.js, index.html, docs/fixtures/optimizer/harness/optimizer-pins.js
Commit: 1274dd0 (hold-first) · e5661af (picker-primary taps) · b101f34 (entity catalogs + auto-defaults, amended design) · 51786f3 (DoC URL merge/promotion)
Decisions: catalog pins live in migrations-catalogs.js to preserve optimizer-engine's no-App-imports contract (harness runs both suites as one command); amendment acknowledged and F3 refactored to it.
Open issues: pre-existing perf-pin flake (beam wall-clock 2206ms vs 2200 ceiling under load — re-run idle in step 3); owner post-release setup (set the two Settings default pairs; confirm catalog reconstituted on first load); iOS blur->focus keypad bounce in DateFieldTap.toTyping unverified on real WebKit — step 2's simulator job.

### 2. Re-verify + Codex adversarial fold  [tier: executor] [codex: embedded-adversarial; pre-gate satisfied by the plan-level critique] [status: done — WITH BLOCKER, see 2b]
Intent: Simulator (real WebKit) + desktop verification of all four behaviors — first-tap/second-tap keyboard states ON DEVICE, DoC URL field surfaced + import round-trip (paste mode at minimum), defaults prefill with synthetic Settings values, hold-first order rendered; full battery; owner-data safety protocol as prior runs (fresh origin, synthetic seed, zero external requests); then codex exec adversarial review (gpt-5.6-sol, scope = the run's commits), fold critical/high with re-verification.
Expected files: fix-up edits only
Outcome: BLOCKER FOUND + everything else green. iOS 26 simulator (hardware keyboard disabled, verified): F1 tap-1 PASS (picker opens, no keypad); F1 tap-2 FAIL — picker closes, field unfocused, NO keypad; DateFieldTap.toTyping's blur->focus bounce doesn't work on WebKit, and two alternates (disarm-on-pointerdown; mousedown preventDefault — works in Chromium, not WebKit) also device-disproved; all experiments REVERTED (tree matches e5661af) rather than shipping a phone that can't type dates. DoC URL surface placement, status/preview outside collapsed disclosure, Settings editor + 4 unset pickers: PASS on device. Desktop Playwright 88/88, 0 console errors, 0 non-loopback requests (7 Worker calls locally mocked). Battery: optimizer 108/109 (sole fail = pre-existing beam-perf flake, 2256-2400ms vs 2200 budget — fails identically on the pre-fold baseline, passes 2165ms idle; box load, not regression; budget left alone), catalog 9/9, feasibility 21/21, parser 24/24, fidelity 67/67, dd-matrix/p2b PASS, node --check clean. PII grep 0 hits. Codex adversarial: FAIL verdict, 3H/3M/1L — 5 findings FOLDED + re-verified in 4a82d8e; rejected H2 (version bump — that IS the release step, noted as MANDATORY there: a mixed cache would fail on DateFieldTap missing from stale dd-widgets.js) and M5 (pointerType rework — unverifiable without hybrid hardware); M4 trade-off accepted (peer offers no longer union via migration; sync adopts peer catalog wholesale).
Files: js/doc-import-templates.js, js/migrations-catalogs.js, js/modals-forms.js, js/offer-model.js, js/optimizer-engine.js
Commit: 4a82d8e (Codex folds; not pushed)
Decisions: reverted all tap-2 experiments on device evidence; beam budget untouched.
Open issues: F1 tap-2 BLOCKS release -> step 2b; pre-existing (not this diff): a modal opened <500ms after boot is wiped by a deferred render — record in BACKLOG at release.

### 2b. F1 tap-2 redesign: "Type" affordance in the picker  [tier: executor] [codex: device-verified-fold] [status: done]
Intent: The owner's literal "second tap raises keypad" is device-impossible (three mechanisms disproved on real WebKit). ELECTED (planner, under standing owner approval; owner notified with veto window): an explicit "Type" control inside the yv-dp popover — a fresh element whose tap handler focuses the (un-armed) field within the same user activation, the canonical WebKit pattern for programmatic keyboard raise. Requirements: visible but unobtrusive (footer row next to Today/Clear is the natural home); tapping it closes the picker, disarms, focuses the field, keypad rises; state machine resets stay coherent (blur/selection/field-switch/modal-close); desktop unaffected (pointer:coarse gate); typed-violation hints intact; DEVICE-VERIFY the full cycle on the simulator (hardware keyboard disabled) incl. re-arming after typing ends. Executed by resuming the step-2 agent (simulator + harness context warm).
Expected files: js/dd-widgets.js, js/date-format-core.js (decision logic), index.html (popover CSS)
Outcome: DEVICE-VERIFIED WORKING — the fresh-element "Type" control raises the iOS keypad where every field-tap mechanism failed. Two-event-turn design: preTypeBlur() on pointerdown over [data-type-entry] disarms + blurs (dd-widgets.js:62-70); the click then closes the picker and toTyping() focuses — a genuine focus move, not the disproved same-handler bounce. Broken tap-2 retired: dateTapDecision coarse+pickerOpen now returns 'noop' (picker stays open; the old path dismissed with no keypad — dead end); pin renamed. Type button coarse-pointer-only, boxed pill 40px min-height. Verified on iOS 26.5 sim (hw keyboard disconnected + proven): tap-1 picker/no-keypad, Type->keypad, violation hint fires + clears, blur re-arms, across offer (funding/sign-up/DD row incl. R90 floor grey), event (date/recurrence end), Settings. Desktop guard 23/23. Batteries: optimizer 108/1 (sole fail = known beam-perf box-load flake, 2269ms), catalog 9/9, feasibility 21/21. PII grep 0. Non-loopback requests 0 (Playwright fence + loopback-bound server log for the simulator side).
Files: js/dd-widgets.js, js/date-format-core.js, js/optimizer-engine.js, index.html
Commit: 5286b0c (not pushed)
Decisions: two-event-turn keypad raise; tap-2 'noop' retirement; coarse-only rendering.
Open issues: DD-row test required flipping a seeded offer to Held+DD (throwaway origin, no cleanup); simulator-side request interception impossible — 0-count rests on the fence + loopback server log.

### 3. Mechanical release v2026.08.23  [tier: worker — RUNG DROP: Codex Luna dispatch wedged 8h41m with a 0-byte output and an untouched tree (headless approval stall, the R91 hang mode, likely compounded by machine sleep); killed and re-dispatched on the config fallback worker (haiku Claude tier-worker). Planner pushes + verifies invariants as review-after] [status: done]
Intent: Bump ×21 + 0-stray scan, CHANGELOG entry, HANDOFF R95 (records: (a) event calendar stays neutral by owner word; hold-first decided+shipped; P1 harness deferred; stable tag still owner-gated at fd37c2c), BACKLOG shipped-markers, then RE-RUN against the final commit: node --check all modules + sw.js, both bare-node batteries, DOM smoke, import-map/precache consistency — only then commit (exact paths) + push. Live-serving verification is step 4's (planner).
Expected files: index.html, js/runtime-status.js, sw.js, CHANGELOG.md, HANDOFF.md
Outcome: Executed by the FALLBACK haiku Claude worker in 3 minutes after the Codex Luna dispatch wedged 8h41m untouched (rung drop recorded above). All applied: bump x21 (0 strays / 19 new ?v= / both APP_VERSION literals), CHANGELOG entry, HANDOFF R95 + Round-95 header, BACKLOG SHIPPED markers + modal-boot-race defect note. Battery vs final tree: node --check 20+sw.js, optimizer 109 + 9 catalog (109 vs briefed 108 — one additional classification pin, all green), feasibility 21/21, parser 24/24, fidelity 67/67, dd-matrix + p2b PASS, beam-perf 4322ms = known box-load flake (budget unchanged), PII grep 0. PLANNER REVIEW-AFTER: independently re-verified staged-file set (exactly 6), stray/count/PII greps, HANDOFF header — all clean. Pushed c86e03f..045d72f; LIVE VERIFIED: runtime-status.js serves 2026.08.23 (second poll, ~40s), index.html serves 19x ?v=2026.08.23.
Files: index.html, js/runtime-status.js, sw.js, CHANGELOG.md, HANDOFF.md, docs/BACKLOG.md
Commit: 045d72f (pushed)
Decisions: fallback rung honored per checkpoint F3 caveat; pin-count delta accepted (explained, green).
Open issues: none.

### 4. Report + owner handoff  [tier: planner] [codex: off] [status: done]
Intent: Compact report; give the owner his 10-second post-release step (paste his two entity/email default pairs into the new Settings fields — exact values from private memory); remind the stable-tag decision remains open (2026-07-29 build, unaffected by this release).
Expected files: none
Outcome: Delivered in chat — release summary, device story, the owner's 10-second Settings setup (values referenced from private memory, not echoed into repo files), open owner decisions (git-history redaction pending; /tiers worker-seat recommendation after two Codex-executor hangs). Run CLOSED.
Files: none
Decisions: live/SW-convergence verification performed by planner per F4 (curl polls; edge served new version in ~40s).
Open issues: none for this run.
