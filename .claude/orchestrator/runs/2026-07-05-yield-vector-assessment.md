---
run: 2026-07-05-yield-vector-assessment
task: "Assess the Yield Vector project for whether it's feasible to integrate a feature within the offer card creation which allows for dynamic pulling of offer parameters via a pasted doctorofcredit URL (e.g. https://www.doctorofcredit.com/il-in-ky-mi-mn-wi-in-branch-only-old-national-bank-500-business-checking-bonus/), so that it adds any currently existing values/dates/etc. into existing fields. Also critique the overall tool and determine if anything could be done better or added that would be additive based on observed scope — the goal is managing dates and requirements as hands-off as possible (figuring out what to do, then remembering to do it). If there's a clear path forward for integrating Apple Reminder creation into the tool, outline that fully as well."
status: done
created: 2026-07-05T00:00:00Z
config_snapshot: { planner: fable, executor: opus, worker: sonnet, codex: { planner: review-before-approval, executor: review-after, worker: review-after } }
plan_approved: true
current_step: 7
---
## Context
Yield Vector is a single-file vanilla JS PWA (~8,000-line index.html) for planning bank-bonus (churning) offers: localStorage persistence, GitHub Gist cloud sync (last-writer-wins base with restore-from-history and stale-push guard per HANDOFF Round ~29), GitHub Pages hosting from main. Offers carry a display-only `docUrl` field; HANDOFF Round 4 deferred a DoC-ingestion analysis. Offer types: `new-funds-held`, `direct-deposit`, `held-and-dd` (no "Other"). An action-queue surface ALREADY EXISTS (`computeActionsRequired()` / `computeUpcomingActions()` ~index.html:4513). Reminders status: the app pushes a `_feed` JSON (kinds: offer-expires, deposit-deadline, withdrawal, commitment-end, inflow, outflow) to the Gist on save, and SHORTCUT_SETUP.md documents a daily 6 AM iOS Shortcut with merge semantics — but per HANDOFF the Shortcut setup on-device is still PENDING/unconfirmed. Treat as "feed shipped, consumer unconfirmed." `tools/create-reminders.swift` is a one-off placeholder (evidence Mac-side EventKit is possible; no architectural weight). This run is an ASSESSMENT — no code changes to the app; final report is written OUTSIDE the repo (scratchpad) to avoid triggering the repo's HANDOFF/commit/push protocol for a non-code deliverable.

## Key decisions
- Codex plan-gate critique (2026-07-05) incorporated: Reminders context corrected to "feed shipped, Shortcut unconfirmed"; report moved out of repo; deliberate DoC sample matrix required; extraction UX must be preview/confirm with confidence + source snippets, never silent autofill; field-mapping matrix must separate existing-field vs new-schema-needed; planned dates are user-specific (DoC gives windows, app derives suggestions); LLM secret handling must be concrete (GitHub Pages can't hold a key; note existing localStorage PAT precedent); critique step must audit existing action-queue/sync reality; stale "Haiku ~$0.01/offer" cost claim must be re-verified against current pricing; synthesis gets a Codex review before delivery.
- Step 1 split to worker tier: sample collection + structure documentation is mechanical and spec-complete; executor keeps the feasibility judgment (step 2).
- Steps 2–4 are executor-tier: judgment-heavy analysis where wrong conclusions propagate into the final recommendation.
- Analysis steps change no files, so per-step `review-after` diff reviews won't trigger; the quality gate for conclusions is the synthesis-stage Codex review in step 5.
- Mid-run scope addition #2 (user, 2026-07-05): user is open to a more capable extraction model than Haiku if it reduces erroneous data entering offers he acts on financially; wants cost differential and value gained. Step 2 amended via follow-up to the same executor agent (model-tier comparison: Haiku 4.5 / Sonnet 5 / Opus 4.8, verified pricing, error-risk analysis vs the preview-confirm safety net).
- Mid-run scope addition (user, 2026-07-05): 18-post real-usage corpus supplied; validation step 5 added (worker) and run IN PARALLEL with step 3 (independent topics) — synthesis renumbered to step 6. User reconfirmed bank-bonus-posts-only constraint; step-1 sample verified compliant.
- Step 1 pivotal findings: DoC posts are fully server-rendered and plain-curl fetchable (no bot-block, 7/7); the "Offer at a glance" `<strong>Label:</strong> value` list has identical labels/order across all samples (best extraction target); glance values go STALE vs top-of-post "Update:" paragraphs (4/7 posts) so recency reconciliation is mandatory; `<del>` strikethrough history appears inline; article-level WordPress taxonomy tags carry state/DD/pull signals; JSON-LD/OG carry no offer fields; ~95% of page bytes are boilerplate. Evidence pack: scratchpad/doc-evidence-pack.md.

## Steps
### 1. DoC evidence pack  [tier: worker] [codex: review-after] [status: done]
Intent: Mechanically gather the raw evidence for the feasibility call. Fetch (curl/WebFetch) the user's example post (https://www.doctorofcredit.com/il-in-ky-mi-mn-wi-in-branch-only-old-national-bank-500-business-checking-bonus/) plus a deliberate sample of 6–8 more current DoC bank-bonus posts covering: held-funds-only, DD-only, held+DD, no/soft expiration, business checking, state-limited and/or in-branch-only, and at least one post with mid-life "Update:" edits. For each post produce: raw fact table (bonus amount, expiration, funding/DD/debit requirements, hold period, availability/states, ETF, monthly fee + waiver, promo code, bonus posting timing, Chex/sensitivity notes, early-close penalty) with the exact source snippet per fact, plus notes on where in the HTML each fact lived (title, bullet list, prose, table, "Update:" block, comments). Also document the page's HTML shell (theme markup, consistent selectors, JSON-LD if any) and whether plain curl retrieves full content (bot-blocking?). NO feasibility judgment — evidence only.
Expected files: none (report only; may stash fetched HTML in session scratchpad, not the repo)
Outcome: Fetched the required Old National Bank post plus 6 additional current DoC bank-bonus posts (Bank of America, Blaze CU, Navy Federal, Park National, Chase targeted, U.S. Bank) via plain curl — all 7 returned HTTP 200 with full article text present in raw server-rendered HTML; no bot-block or JS-hydration was encountered anywhere, so WebFetch fallback was never needed. Sample achieves all 7 requested categories (held-funds-only, DD-only, combined, no/soft-expiration, business checking, state-limited/in-branch, mid-life Update edits), with fact tables and structural/HTML-shell documentation captured.
Files: scratchpad/doc-samples/post1..post7 HTML + *_article.html extracts + category_page.html; full evidence pack persisted by planner to scratchpad/doc-evidence-pack.md. Nothing written to the repo.
Decisions: Used /category/bank-account-bonuses/ archive for discovery; posts chosen to span all 7 categories with noted overlaps (Park National = state-limited + hold/DD + blank-EATF; U.S. Bank = business + held-only + heaviest Update history).
Open issues: wpdiscuz comments not sampled structurally; conflicting-data-point language thin in this sample (Blaze pull-type, Chase DD-routing only) — deliberate comment mining would be a follow-up if that signal matters.
Commit: n/a (no repo changes)

### 2. DoC URL parsing feasibility and design  [tier: executor] [codex: review-after] [status: done]
Intent: Consume step 1's evidence pack. Read HANDOFF.md Round 4's deferred analysis, the offer data model (~index.html:6843) and modal fields (~index.html:6294). Produce: (a) field-mapping matrix — DoC fact → existing field | derived field | NEW SCHEMA NEEDED, with per-field extraction confidence and reminder impact; (b) honest client-side constraint analysis for a GitHub Pages origin (DoC CORS absent): CORS proxy, LLM extraction via API (verify CURRENT pricing — do not reuse the archived ~$0.01/offer claim), user-supplied API key in localStorage (precedent: Gist PAT), iOS Shortcut/share-sheet-assisted fetch, paste-page-text fallback, local Node tool (precedent: tools/build-dd-methods.js); (c) 2–3 concrete implementation designs with reliability/effort/cost/secret-handling tradeoffs and one recommendation; (d) UX acceptance criteria: paste URL → preview extracted fields with confidence + source snippet → user confirms; never overwrite non-empty fields without confirmation; parsed terms fill offer PARAMETERS, then the app derives suggested plan dates (planned signup/funding/DD dates stay user-owned); extraction failures route through logError/diagnostics. (e) Explicit verdict: feasible / feasible-with-caveats / not worth it.
Expected files: none (analysis report only)
Outcome: Delivered the full DoC-ingestion feasibility analysis (field-mapping matrix, transport analysis with re-verified Haiku 4.5 pricing at ~$0.0065/offer, three implementation designs, UX acceptance criteria, and verdict). Verdict: feasible-with-caveats and worth building, deterministic glance-list parser first (worth shipping alone), Cloudflare-Worker + optional Haiku assist tier second. Grounded against the live offer model, form/readOfferForm, ErrCode/logError infra, syncConfig PAT precedent, and DDMethods fuzzy bank matching; no files changed. Full deliverable: scratchpad/step2-doc-feasibility.md.
Files: none (repo untouched)
Decisions: Deterministic paste-text tier is the shippable v1 (glance list = highest-confidence target, zero infra/secrets/cost); ~11 NEW-SCHEMA facts routed to notes for v1; Worker-held API key preferred over localStorage (isolation + solves fetch CORS in one hop); "parsed facts → parameters only, dates stay user-owned" enforced throughout; Haiku cost re-derived from current $1/$5 MTok pricing.
Open issues: (1) which NEW-SCHEMA facts graduate to dedicated fields with reminders (bonus-posting timing, ETF safe-close, state eligibility); (2) regression corpus via tools/parse-doc.js; (3) tiered-value pre-select UX; (4) confirm Cloudflare Worker acceptable ongoing infra before v2.
ADDENDUM (user scope addition #2, same executor agent resumed): Model-tier comparison delivered with pricing reconfirmed from official docs (Haiku 4.5 $1/$5, Sonnet 5 $2/$10 intro→$3/$15, Opus 4.8 $5/$25). Recommendation changed from Haiku to **Sonnet 5** for the extraction tier (differential ~$0.0065/offer, <$1/mo at 50 offers; accuracy prioritized on financially-actionable data; Opus not worth 2.5× — ceiling is source ambiguity, not model capability), with a mandatory per-field verbatim-snippet verification tripwire (programmatic substring check against page text) ranked as the real safety mechanism alongside preview/confirm UX and deterministic-first layering. Full addendum: scratchpad/step2b-model-tier-addendum.md.
Commit: n/a (no repo changes)

### 3. Whole-tool critique and additive recommendations  [tier: executor] [codex: review-after] [status: done]
Intent: Critique Yield Vector against Collin's stated goal — "managing dates and requirements as hands-off as possible: figuring out what I need to do, then remembering to do it." Ground in current reality: audit the EXISTING `computeActionsRequired()`/`computeUpcomingActions()` surface (~index.html:4513) and propose extensions rather than re-inventions; describe sync as it is now (Gist LWW + restore-from-history + stale-push guard) and assess residual risk; review offer lifecycle (subStatus enum), deadline derivations (depositDeadline/withdrawalEligibleDate/lockStartDate), business-day/ACH-holiday logic, dd-methods suggestion flow, error/diagnostics surface, single-file maintainability. Exclude auto-push.js from the product critique (dev-workflow; one line max as a repo-process note). Do not propose re-tuning locked design values (CLAUDE.md Round 36). Deliver: (a) what's already strong; (b) weaknesses/risks ranked by user impact; (c) additive feature proposals ranked by value-vs-effort, each tied to the hands-off goal.
Expected files: none (analysis report only)
Outcome: Delivered a three-part whole-tool critique (strengths, ranked weaknesses, ranked additive features) judged against Collin's hands-off goal, with file:line references for every code claim. Verified the three most load-bearing findings by tracing the actual code: debitRequirement.byDate and DD-window-end never reach computeUpcomingActions/computeReminderFeed; on-track is a manual label with no computed risk logic; and the deposit-deadline reminder is gated on legacy status applied|selected|prospect while deriveLegacyStatus maps Approved offers to 'funded', hiding that reminder exactly when funding is pending. Full deliverable: scratchpad/step3-tool-critique.md.
Files: none (repo untouched)
Decisions: Ranked by user impact; single-file architecture scored "fine, don't refactor"; features sequenced on P1→P3→P2→{P4,P5,P7} spine (at-risk tracking needs done-tracking needs trustworthy queue); sync assessed as residual risk per ground rules; DoC/Reminders/visual excluded per scope.
Open issues: Deadline-consumer sweep grep-verified but not runtime-verified; no partial per-DD completed flag found; iOS localStorage eviction asserted from platform knowledge, not device-tested.
Commit: n/a (no repo changes)

### 4. Apple Reminders integration assessment and full path forward  [tier: executor] [codex: review-after] [status: done]
Intent: Audit the Reminders pipeline as "feed shipped, consumer unconfirmed": SHORTCUT_SETUP.md end-to-end, `_feed` emission sites in index.html, merge/deletion semantics, and what confirms (or not) the Shortcut is actually installed/running. Coverage audit: compare current feed kinds against the full actionable-date universe (each planned DD initiation, debit-requirement deadline, expected bonus payout, safe-to-close date, monthly-fee actions, ETF/early-close dates) and list gaps. Failure-mode analysis: Shortcut never installed or silently stops, Gist auth expiry, feed/consumer divergence, no delivery confirmation loop. Then outline the full path forward: hardened Shortcut pipeline vs alternatives (Mac-side EventKit Swift CLI — create-reminders.swift as feasibility evidence only, osascript, reminders:// or x-callback deep links), one recommended architecture with concrete integration points, setup steps, and maintenance cost. Scope the DoC tie-in correctly: a parsed offer flows into reminders automatically only AFTER the user confirms their plan dates.
Expected files: none (analysis report only)
Outcome: Audited the Apple Reminders integration as designed-but-unverified across app→_feed→Gist→Shortcut→Reminders; confirmed the pipeline has no back-signal, so every failure (starting with the currently-unbuilt Shortcut, per HANDOFF_ARCHIVE:1059) degrades silently. Recommended architecture: harden the existing Gist+Shortcut pipeline with a consumer-heartbeat write-back plus the three coverage-gap fixes; the single next action is to build and verify the Shortcut on-device before any new engineering. Verified all three step-3 gaps at file:line, plus the safeToCloseDate dead stub (:3560, always null), absent monthly-fee/bonus-posting fields, the empty-feed mass-delete risk in the Shortcut spec, the silent try/catch around feed computation (:2556), and the includeInScenario gate that structurally enforces the DoC user-confirmation rule. Full deliverable: scratchpad/step4-reminders-audit.md.
Files: none (repo untouched)
Decisions: Reframed failure analysis around detectability (all modes collapse to the same silent symptom); recommended hardening over Mac EventKit/launchd (fragility) and deep links (violates hands-off); mapped step-2's NEW-SCHEMA facts to two concrete new feed kinds (bonus-posting-check, safe-to-close).
Open issues: Consumer half asserted from spec, not observed; raw.githubusercontent cache delay untested; exact replacement for the :4450 legacy gate is a recommendation, not implemented.
Commit: n/a (no repo changes)

### 5. Corpus validation against Collin's real posts  [tier: worker] [codex: review-after] [status: done]
Intent: Mid-run user addition (2026-07-05): Collin supplied 18 DoC URLs of offers he has actually done and stressed the sample must be bank-bonus posts only (verified: all 7 step-1 posts were). Validate the step-2 parser design against this REAL corpus: fetch each, confirm post type, check "Offer at a glance" presence and label conformance vs the canonical 11-label set, and flag anything that breaks step-2 assumptions — especially non-dollar denominations (Amex 50k MR points, Chime 10,000 Swagbucks), fintech offers (Mercury, Relay, Chime, SoFi), the BMO checking+savings combo, and the pre-announced Bask "live from 11/1/25" post. Produce a compatibility table + list of required design amendments.
Expected files: none (report only; HTML stashed in scratchpad/doc-samples-user/)
Outcome: All 18 URLs fetched successfully (HTTP 200/18). Assumptions A1 and A2(label-set)/A3(presence)/A4(heading-presence) mostly held (15-17/18) but each broke on identifiable posts; A5 (taxonomy classes) broke on Wintrust (zero tag classes); A6 (dollar amount) broke on Amex (points-denominated). Full compatibility table, per-assumption break analysis with markup snippets, and 8 required design amendments compiled (fuzzy label matching, missing-ChexSystems tolerance, points/range/combined amounts, non-dated status lines + unsorted updates, tolerant h2 checks, zero-taxonomy handling, del-stripping). Full deliverable: scratchpad/step5-corpus-validation.md.
Files: scratchpad/doc-samples-user/{1-18}.html + _entry/_glance extracts (repo untouched)
Decisions: entry-content extracted via bounded regex per spec; 3 ambiguous cases (BMO dual-bonus, Chime SB-vs-USD, Relay h2 set) spot-read rather than grep-trusted.
Open issues: Update-block chronology exhaustively verified on only 3 posts; "Contents" TOC blocks may interact with a naive glance-boundary rule — flagged for implementer.
Commit: n/a (no repo changes)

### 6. Reminders framework redesign — Fable first-principles design + Codex cross-review  [tier: planner] [codex: review-before-approval-style cross-check] [status: done]
Intent: Mid-run user addition #3 (2026-07-05): Collin is open to a NEW vision/framework for Reminders — the existing Gist+Shortcut design predates this run and he explicitly trusts the planner model's (Fable's) design judgment, cross-referenced by Codex. Planner designs a from-first-principles reminder-delivery architecture using step 4's audit as the constraint set (no back-signal = root failure; coverage gaps; static-PWA + no-backend reality; Apple ecosystem), verifies load-bearing external assumptions (CalDAV/iCloud Reminders accessibility, iOS calendar-subscription behavior, Shortcuts HTTP capabilities) via web search, then runs `codex exec` critique over the draft design and incorporates findings before it enters the final report.
Expected files: scratchpad design doc only (no repo changes)
Outcome: Designed the "One Brain, Three Surfaces" Reminders framework from first principles: all logic in the app's feed engine (contract v2 with tombstones, schema versioning, risk states, complete coverage), three redundant delivery surfaces (in-app queue = source of truth; zero-maintenance ICS calendar subscription served by the shared Cloudflare Worker; minimal ~10-action dumb-executor Shortcut for actual Reminders), heartbeat write-backs with a cross-channel watchdog (stale Shortcut heartbeat → warning event injected into the calendar feed). Load-bearing platform facts verified by web search first: CalDAV cannot write to post-iOS-13 Reminders (kills any server-push vision; on-device EventKit is the only path), ICS subscriptions auto-refresh hourly-ish, Shortcuts can PATCH with headers/body. Codex cross-review (113k tokens) attacked it; 10 amendments incorporated (v1.1): liveness claim downgraded to "observable last successful run" with documented residual-silence list, per-consumer heartbeat files, ack-based tombstone retention, delete-rate guards, stable child IDs, owned-list semantics, monotonic manifestVersion surviving restore-from-history, version-lag alerting, ICS hygiene + capability-URL rotation/redaction, reentrancy-locked dual triggers. Full design: scratchpad/step6-reminders-redesign.md.
Files: scratchpad/step6-reminders-redesign.md (repo untouched)
Decisions: Kept the Shortcut as the Reminders writer (C1 forces it) but demoted it to dumb executor; calendar channel chosen as the reliability floor AND the alarm bell for the Reminders channel; Worker shared with DoC import (one infra piece, two features); legacy SHORTCUT_SETUP.md design superseded rather than hardened.
Open issues: ICS alarm behavior across iOS versions accepted as residual (timed events mitigate); redacted-titles mode is optional pending Collin's privacy preference; heartbeat store could consolidate to a Worker merge endpoint if per-consumer Gist files feel sprawling.
Commit: n/a (no repo changes)

### 7. Synthesis, Codex review, final report  [tier: planner] [codex: review-after] [status: done]
Intent: Planner merges steps 1–6 into one coherent assessment: DoC-parse feasibility verdict + recommended design, tool critique with ranked additive features, full Reminders path forward, and a suggested implementation sequence if greenlit. Draft the report in the session scratchpad (NOT the repo), run `codex exec --sandbox read-only` critique over the draft, fix substantiated findings, then deliver via SendUserFile plus a compact chat summary.
Expected files: scratchpad report file only (no repo changes)
Outcome: Final report synthesized from all six streams and Codex-reviewed. Codex's editorial pass produced two factual corrections that were fixed before delivery: (1) the DD window-end deadline IS emitted but conflated with the generically-labeled deposit/funding item and hidden by the same Approved-status gate (report claimed "never emitted"); (2) the DoC user-confirmation gate does NOT exist today — new offers default includeInScenario:true, so feed v2 must ADD the needs-confirmation gate for imported offers (report claimed it was structurally enforced). Also incorporated: tripwire reframed as source-grounding (not hallucination-proofing) with an added value-in-quote consistency check recommendation; mass-delete blast radius scoped to planner-managed reminders; calendar-channel liveness wording softened (eventual refresh, no SLA); Shortcut effort estimate widened to ~30–60 min; cost-spread wording clarified as differential ($0.26–$1.30/mo) vs total (≤$1.63/mo). Report delivered to Collin via SendUserFile.
Files: scratchpad/yield-vector-assessment-report.md (repo untouched)
Commit: n/a (no repo changes)
