---
run: 2026-07-07-bonusflow-feature-implementation
task: "all 5 features carefully, thoughtfully, & strategically to provide a well-designed, improved version of what BonusFlow provides. Ensure there is no break down of aesthetics/formatting in implementation of new features, rather, take this as an opportunity to sharpen the aesthetics/formatting as it seems appropriate to do so. Carry forward through the night without asking for permissions as I'm going to bed. If something needs reversion: I assume it can be accomplished through github state recovery at the very least. Also implement a way for offers to record churnability/i.e. when they can be ran again, and have upcoming Churn dates show in the overview toward the bottom somewhere."
status: in-progress
created: 2026-07-07T00:05:00-05:00
config_snapshot: { planner: fable, executor: opus, worker: sonnet, codex: { planner: review-before-approval, executor: review-after, worker: review-after } }
plan_approved: codex-findings-noted (user pre-authorized overnight autonomy — no approval questions)
current_step: 9
---
## Context
Implement into Yield Vector (`/Users/collinrekowski/Automation/Yield Vector`, single-file vanilla-JS PWA `index.html` ~9.3k lines, localStorage + private Gist sync, GitHub Pages deploy from main, APP_VERSION `2026.07.07b` @ ~:2401) the 5 features recommended by run `~/.claude/orchestrator/runs/2026-07-06-bonusflow-yv-feature-review.md`, plus churnability tracking. Source designs: BonusFlow evidence (that run's step 3) + YV's `docs/assessments/2026-07-05/report.md` (Phase 6/7 DoC import design). Collin is asleep: FULL AUTONOMY authorized — no permission questions; GitHub state is the agreed recovery mechanism. Constraints: zero-infra/zero-secret/offline-capable; no personal/financial data in public repo; reminders `_feed` consumed by an iOS Shortcut (guide: `docs/SHORTCUT_BUILD_GUIDE.md`) — changes must be additive AND guide-updated; pending UNRELATED working-tree changes (`.claude/settings.json` M, `.codex/hooks.json` D, `AGENTS.md` M, `CLAUDE.md` M = Collin's R62-R64) must NEVER be committed, reverted, or modified; AGENTS.md must NOT be edited this run (dirty) — durable facts go in the HANDOFF entry instead.

## Key decisions
- Overnight autonomy (Collin, verbatim): Codex plan gate ran, findings incorporated (below), NO user approval question. Step failures: retry once same tier w/ failure context → escalate one tier / planner-narrowed retry → if still failing mark failed, skip hard-dependents, continue independents. All recorded here.
- Runs dir: repo-local `.claude/orchestrator/runs/` (matches assessment-run convention).
- Sequential steps only (single-file monolith); scoped `git add` per step (never R62-R64 files).
- **Push policy (revised per Codex):** commit per step LOCALLY; push to origin ONLY after step 11 verification passes. Rationale: GitHub Pages auto-deploys main and Collin's phone PWA + Gist sync could ingest a half-migrated schema overnight; local commits still survive crashes and preserve per-step history. If verification fails: no push, live site stays on `7c06e5f`, morning report explains. Safety tag `checkpoint-2026-07-07-pre-bonusflow` (NOT `stable-*` — reserved for owner-confirmed builds) at `7c06e5f`, pushed immediately.
- **Layering, not replacement (Codex critical):** legacy fields (`offerType`, `directDeposits`, `ddRequirement`, `debitRequirement`, hold/funding fields) remain canonical for existing consumers (`buildReminderItems` ~:5371, `renderOfferCard` ~:5984, `readOfferForm` ~:8063, projection/ROI/timeline). `requirements[]` layers on top: legacy DD/debit fields are auto-derived INTO requirement rows (and kept in sync) rather than deleted. Step 2 starts with a written consumer-inventory design doc before code.
- **Migration safety (Codex critical):** idempotent, non-destructive, pre-migration localStorage backup key + Settings restore path; unknown keys preserved BOTH in migration and in `readOfferForm` save path (merge-spread prior object — current save constructs a fixed object at ~:8103 and silently drops extras); migrated state NOT auto-pushed to Gist on first load (push only after first user-initiated save, per existing debounced-save semantics); storage-version guard so older app builds don't clobber v2 payloads (extend existing bilateral sync guard pattern).
- **Field promotion trimmed (Codex):** promote only fields with a UI consumer THIS run: monthly_fee + fee_waiver_condition, promo_code, early_termination_fee + etf_window_days, bonus_post_min_days/max_days, expiration date (if not already first-class), last_edited timestamp, churn fields + anchor dates (bonus_received_date, closed_date). state_eligibility + credit_inquiry stay in notes (no consumer yet).
- **Lifecycle = derived view (Codex):** the 4-stage pipeline maps over the EXISTING `accountStatus` + 9-value `subStatus` model (denied/archived/prospect etc. render as badges outside the pipeline), no status-model fork. `safeToCloseDate()` implements the intentional null stub at ~:4433 with `max(withdrawal-eligible, expected-bonus-posted+buffer, ETF window end, all requirements resolved)`.
- **Feed changes are versioned-additive:** stable per-requirement IDs (avoid the solved DD-id tombstone bug class); new feed kinds documented; fixed-seed before/after `_feed` JSON diff required in steps 3-5; `docs/SHORTCUT_BUILD_GUIDE.md` updated in step 11.
- **Per-step gates (Codex):** JS syntax check (extract inline `<script>` → `node --check`), preview console clean, mobile ~380px smoke on UI steps, `git status --short` review before every commit.
- DoC import v2 = gated scaffold (not fully "inert": settings field + hidden-until-configured URL mode): Worker source + deploy docs committed; zero secrets; zero network calls unless Worker URL configured. Deploying = Collin's morning task. Step 7 parser gets checked-in SYNTHESIZED fixtures replicating the 8 DoC quirks (no wholesale copying of DoC content; cite source URLs).
- Retiered per Codex: step 6 worker→executor (sort/filter state doesn't exist yet — new state + UI + schema touch); step 8 worker→executor (Worker security/CORS/prompt-validation/client gating).
- Codex tier suggestion "step 2 needs planner design substep": adopted as design-doc-first REQUIREMENT inside step 2; planner reviews the design doc at transcription before permitting step 3.
- Templates (step 9) must strip ALL personal instance data incl. entityUsed, emailUsed, notes, dates, completion state, churn notes (Codex: original list was incomplete; "last-4" doesn't exist as a field — do not invent it).
- APP_VERSION: bumped ONCE in step 11 (target `2026.07.08a`) + CHANGELOG entry; HANDOFF current-state block is stale (says 2026.07.07a) — step 11 corrects it in the new round entry.
- CODEX OUTAGE (from step 3 post-review onward, ~02:15): workspace out of credits again (plan critique + step-2 review consumed the reset; companion reviewer failed 2×, codex exec probe confirms). Substitution per autonomy grant: each remaining step's review-after gate = fresh-context ADVERSARIAL REVIEW by an independent Opus agent (not the implementer) over the step diff, P1/P2/P3 findings, criticals fixed before commit — same discipline, different reviewer. Morning follow-up for Collin: add Codex credits and run a full-diff Codex review (checkpoint tag → HEAD) to backstop; steps 2 kept its real Codex review.

## Steps
### 1. Baseline safety + conventions digest  [tier: worker] [codex: review-after] [status: done]
Intent: Create+push tag `checkpoint-2026-07-07-pre-bonusflow` at `7c06e5f`. Produce a dense conventions digest for injection into all later prompts: verify/expand these anchors — APP_VERSION ~:2401; feed builder + contract ~:5293 & `buildReminderItems` ~:5371; display↔storage helpers ~:4025 (`formatDateDisplay`/`parseDateInput`/`formatMoneyInput`/`parseMoneyInput` — NEVER bare-parse `data-money`/`.yv-date` fields); `.field-box` label rules ~:1672 (R61 min-height interaction — do not reintroduce); `.btn-grid`/`.btn-outline-danger` (R60); `renderOfferCard` ~:5984; `readOfferForm` ~:8063 (fixed-object save); `safeToCloseDate` stub ~:4433; `accountStatus`+`subStatus` enum values; offers search/status-filter state (no sort exists); storage/version/sync-guard mechanism + Gist push semantics; demo seed ~:9209; CSS design tokens (colors/spacing/letterspacing 0.02em convention); HANDOFF round-entry template; how to run preview (`.claude/launch.json` = `yield-vector-static`).
Expected files: none (tag only)
Outcome: Tag `checkpoint-2026-07-07-pre-bonusflow` created at `7c06e5f` and pushed. Digest delivered (planner holds full copy for prompt injection). Load-bearing corrections: APP_VERSION is `2026.07.07b` (:2401; HANDOFF header stale); feed payload = `{schema:2 EXACT, generatedAt, manifestVersion, feedStatus, lastGoodGeneratedAt, items:[{id,kind,title,dueDate,notes}], removed}` w/ tombstones via `_feedEmitted`/`_feedRemoved` (90d TTL) and Shortcut matching by `https://yieldvector.local/id/<id>`; kinds: offer-expires|deposit-deadline|dd-initiate|dd-window-end|debit-deadline|withdrawal|commitment-end|inflow|outflow; NO numeric storage version — migrations are functions in App.init (migrateDdIds/migrateDebitRequirement pattern); sync = `_baseRevision` CAS (no code constant for "2026.07.05"); push = Sync.schedulePush 2.5s debounce, no push-on-load; STORAGE_KEY `capital-planner-v1`; ACCOUNT_STATUSES [open,closed] :2516, SUB_STATUSES 9 (:2518): prospect,applied,approved,denied,on-track,met-waiting,earned,didnt-track,archived; surfaces = card grid (renderOfferCard :5984), advanced table (:6310), edit modal (showOfferModal :7455, Advanced toggle :7728), NO detail route; readOfferForm :8063-8134 fixed-object (drops unknowns), ends normalizeOfferStatus; safeToCloseDate stub :4433-4439; helpers: display/storage boundary :4088-4156, usFederalHolidays :3634, directDepositEffectiveDate :3752; tokens :23-88 (--accent #5b5cf6, --danger #e87171, radii sm8/md12/lg16/xl20/pill999); .btn-grid :1265; .btn-outline-danger :458; letterspacing 0.02em; field-box label fix near :1672; offers controls = search+status filter, NO sort; preview = `yield-vector-static` port 4173; package.json watch script = auto-push.js (unreliable, never rely on it).
Files: read-only (index.html, HANDOFF.md, CHANGELOG.md, docs/SHORTCUT_BUILD_GUIDE.md, .claude/launch.json, package.json; AGENTS.md grep-only)
Commit: none (tag only — no file changes; codex review-after vacuous)

### 2. Schema v2 design doc + data model + migration  [tier: executor] [codex: review-after] [status: done] (agents leave changes uncommitted; planner commits after codex post-review)
Intent: FIRST write `docs/assessments/2026-07-07-schema-v2-design.md`: consumer inventory (every reader/writer of legacy offer fields), layering strategy (legacy⇄requirements[] sync rules, which side is canonical per consumer), migration plan + rollback, feed-impact table. THEN implement: `requirements[]` rows ({id: stable, type: enum(spend|deposit|direct_deposit_amt|direct_deposit_count|transactions|debit_txns|activate_debit|estatements|online_banking|maintain_balance|promo_code|custom), amount, count, deadline_days, frequency(total|monthly|per_statement), hold_days, done, done_date, notes}); churn fields ({churnable: bool|null, churn_wait_months, churn_anchor: enum(bonus_received|account_closed|account_opened), churn_notes}) + anchor date fields (bonus_received_date, closed_date — reuse existing if present); trimmed promoted fields (per Key decisions); last_edited; storage version bump + idempotent migration (legacy DD/debit/funding → derived requirement rows; backup key `yv_backup_<oldVer>`; unknown keys preserved); `readOfferForm` merge-spread fix; Settings restore-from-backup; sync-guard extension; demo seed updated. Verify: preview — seed old-shape data via console, reload, assert converted + backup + idempotency (2nd reload no-op); syntax gate; console clean.
Expected files: docs/assessments/2026-07-07-schema-v2-design.md, index.html
Outcome: Shipped additive schema-v2 layer: `requirements[]` rows layered over canonical legacy fields via pure `deriveRequirementsFromLegacy` + in-place `syncRequirementsWithLegacy` (derived rows keep done/done_date/notes, stable ids `req-funding`/`req-ddreq`/`req-dd-<ddId>`/`req-debit` reusing migrateDdIds ids); 13 new scalar fields (churn quartet + anchor dates, monthly_fee+waiver, promo_code, ETF+window, bonus_post_min/max, last_edited) via `schemaV2Defaults()`; idempotent `migrateOffersToSchemaV2` (marker = presence of offer.requirements; one-time quota-safe `yv-backup-pre-v2` snapshot; NO save/schedulePush — verified `_dirtySince` stays null); `readOfferForm` now spreads prior offer first (unknown-key preservation) + stamps last_edited + syncs rows; Settings restore button (conditional, btn-outline-danger) round-trips. Seeds: US Bank churnable/12mo + PNC promo/fee examples; rest left legacy to exercise migration. All 5 gates passed: node --check, 21/21 logic assertions, console clean ×3 reloads, live-seed migration + byte-identical idempotency, git status scoped. Design doc: docs/assessments/2026-07-07-schema-v2-design.md (377 lines; §B write-through rule, §F feed-impact table for steps 3-5).
Files: index.html (+328/-3, new fns ~:2650, App.init hook, readOfferForm, Settings); docs/assessments/2026-07-07-schema-v2-design.md (new)
Commit: 46c0c49 (index.html + design doc + this checkpoint; local only per push policy)
Codex post-review (working-tree): [P1] erase-all doesn't remove yv-backup-pre-v2 → erased financial data resurrectable via restore button; [P1] cloud pull/adopt path can install pre-v2 Gist payload WITHOUT migration (boot-only hook) + readOfferForm prior-spread doesn't backfill v2 scalars → fix: migrate at every adoption point + spread schemaV2Defaults first; [P2] restore writes old _lastModified → next pull silently re-adopts newer cloud, undoing confirmed restore → stamp per _baseRevision CAS semantics. All three dispatched as fix-up to same executor (agent a59af976c3ce60c03) before commit.
Fix-up outcome (verified, gates re-run): clear-all now calls new `clearPreV2Backup()` (~:2916) + button re-hides; `migrateOffersToSchemaV2` inserted at all 8 adoption sites (resolveFirstSync.adoptCloud ~:3213, safeSync.adoptRemote ~:3339, safeSync fresh-pull ~:3375, Sync.pull ~:3442, push CAS adoptCloud ~:3534, restoreState ~:3638, sync-precheck ~:9448, importJsonFile ~:9511) — per-site insertion chosen over choke-point wrapper (bespoke lineage/toast handling at each site); readOfferForm spread now `{...schemaV2Defaults(), ...(prior||{})}`; restore is sync-aware — migrates snapshot in memory, fresh `_lastModified`, delegates to audited `Sync.restoreState` (clears _baseRevision, force-push = new cloud head), backup consumed on success; unconfigured branch keeps reload path. Accepted agent judgment: `reset-sample` intentionally does NOT clear backup (softer action, keeps recovery point).
Agent open issues → carried forward: (1) modal open materializes default ddRequirement (existing :7489 idiom) → saved offers gain a derived req-ddreq row — step 3 owns modal defaults; (2) CRITICAL for steps 3-7: edits to derived rows MUST write through to the canonical legacy field or next sync overwrites them (design doc §B).

### 3. Requirements engine UI + computed dates  [tier: executor] [codex: review-after→substituted adversarial review] [status: done]
Intent: Offer form: dynamic requirement rows (add/remove, 12-type dropdown, amount/count, deadline-days, frequency) with live computed calendar deadline dates from Account Open Date beside each row; legacy DD-specific inputs remain but are visually unified with (and kept in sync with) their derived rows per step 2's design. Card surface (existing expanded-card/modal per step 1 digest — NO invented detail route): requirements checklist w/ strikethrough-on-complete + per-row done toggles feeding `_feed` additively with stable requirement IDs + tombstone semantics matching existing patterns. Fixed-seed feed before/after diff must be additive-only.
Expected files: index.html
Outcome: Shipped: modal Requirements section (all 12 types, add/remove user rows, live computed dates anchored on plannedSignupDate — same anchor as depositDeadline/debitDeadlineISO; derived rows write-through via the existing legacy DOM inputs so readOfferForm+sync re-derive; reverse-sync on `change` to avoid loops; user rows ride hidden `#f-user-reqs` JSON merged in readOfferForm); ddRequirement materialization root-cause fix (derivation gates req-ddreq on DD-family offerType + modal uses render-only `ddReqView`, no live-offer mutation); card checklist w/ tap-toggle done (strikethrough, done sorts below pending, persists via normal save) + table met/total chip; feed: `requirement-deadline` kind ONLY for source:'user' rows on committed offers, id `yv-offer-<id>-req-<rowId>`, done rows auto-tombstone; contract comment updated additively. Gates: node --check ✓; console clean; FEED DIFF: before/after byte-identical for demo seed (9 items), new kind appears only for user rows; mobile 380px no h-scroll ✓. New fns ~:2676 helpers, ~:8480-8830 render/logic, toggleRequirementDone ~:9788, feed block ~:5990.
Files: index.html only
Commit: 9757317 (local)
Post-review: codex OUT OF CREDITS (companion failed 2×, exec probe confirmed) → substituted independent adversarial Opus review (agent ab6c180786112a3c9): VERDICT 0 P1, 0 P2, 1 P3 — [P3] freq-mode derived DD-count row edit silently discarded (:8629 maps count→#ddreq-count-n unconditionally; freq mode derives from freqPeriods) → assigned to step 10 sweep (make that field read-only in freq mode or map to freq-periods input). Reviewer explicitly cleared: no write-through loops, XSS-safe (escapeHtml/escapeAttr consistent), feed envelope frozen, derived rows never emit, tombstones correct, no handler leaks.
Agent open issues → carried: readOfferForm still persists inert ddRequirement on non-DD offers (suppressed by derivation gate — optional later cleanup); user-row hold_days/notes not surfaced in modal editor (rounds-trip fine).

### 4. Lifecycle pipeline + Expected-Bonus window + safe-to-close (F3)  [tier: executor] [codex: review-after→substituted adversarial review] [status: done]
Intent: 4-stage pipeline strip (Meeting Requirements → Waiting for Bonus → Bonus Earned → Closed) as a DERIVED view over existing accountStatus/subStatus (edge statuses = badges, not pipeline stages); auto-suggest transition when all requirements done; Expected-Bonus window = last-requirement-met date + bonus_post_min/max_days; implement `safeToCloseDate()` stub with max-of-constraints formula (Key decisions); surface both on card + additive `_feed` items; feed diff gate.
Expected files: index.html
Outcome: Shipped derived lifecycle: stage mapping closed→approved/on-track→met-waiting→earned else inactive-strip+chip; pipeline strip on card (between checklist and status chip) + slim modal variant above status selects; auto-suggest one-tap →met-waiting w/ persisted per-offer dismiss (`lifecycle_suggest_dismissed`), never automatic; expected-bonus window = latest done_date (fallback today, "estimated") + explicit days or DEFAULT 90-105 ("typical"); safeToCloseDate = null-safe ISO max(withdrawalEligibleDate, bonus_received_date|window end, plannedSignupDate+etf_window_days, all requirementDeadlineISO); feed kinds `expected-bonus-window` (met-waiting only, id yv-offer-<id>-bonuswindow) + `safe-to-close` (open ∧ {earned,met-waiting}, id -safeclose); closed_date stamped on save-path status change (never overwrites); bonus_received_date exposed in modal Advanced (.yv-date). Gates: node --check ✓, console clean, full status walkthrough ✓ (explicit 35-45d US Bank seed + default-fallback Citi), feed diff: pre-existing set byte-identical, new kinds only under exercised states, tombstone-on-leave ✓, monotonic manifestVersion ✓, inactive statuses sane, 380px fits, git scoped. 28-assertion unit test of pure fns passed. New fns ~:5008 (lifecycle module replacing stub), renders ~:6740, CSS ~:1310, feed ~:6297/6320, modal ~:8620/8685/9140.
Files: index.html only
Commit: b389c18 (local)
Substituted review (agent a55bee70feacc8899, Node harness-verified): 0 P1, 2 P2, 3 P3 — [P2] done rows' deadlines constrain safeToCloseDate (pushes past bonus_received); [P2] card safe-to-close line gated only on open (renders for approved/denied; feed gates earned|met-waiting); [P3] past window end emits stale dueDate (existing no-past-filter policy — card copy fix only); [P3] closed_date never cleared on reopen (stale churn anchor risk); [P3] suggest fires on closed offers. Reviewer refuted: XSS, handler leaks, closed_date overwrite-per-save, dup feed ids, version monotonicity, CSS collisions. All 5 dispatched as fix-up to implementer (a63d20c92d64b2304).
Fix-up outcome (verified, gates re-run, 7-assertion unit test): !done skip in term (d) (propagates to feed kind — intended); card gate = open ∧ {earned,met-waiting} (matches feed); overdue copy "Bonus overdue — window ended <date>" in #b45309 (.yv-life-v.warn, only while met-waiting; earned shows received date); reopen (prior closed → open) clears closed_date via else-if before stamp branch (never re-stamps same save); suggest requires open. Feed still additive-only, envelope frozen, fresh-seed counts byte-identical.
Agent open issues → carried: modal slim-strip live preview shows fallback window while editing (bonus_post days + done_date not form-exposed; card is authoritative post-save — acceptable, documented in-code); promoted scalars (monthly_fee, promo_code, etf_window_days, bonus_post_min/max) still form-unexposed → AMENDMENT: step 6 will expose them in modal Advanced (bounded: data-money/.yv-date conventions); pre-existing FAB overlap at 380px bottom-scroll noted (not ours).

### 5. Churnability + upcoming-churn overview section (F6)  [tier: executor] [codex: review-after→substituted adversarial review] [status: done]
Intent: Next-eligible churn date = anchor date (bonus_received_date | closed_date | open_date per churn_anchor) + churn_wait_months; card shows churn status line (eligible now / eligible <date> / not churnable / unknown); NEW "Upcoming churn dates" section near the BOTTOM of the main overview: offers eligible now or within horizon (default 90d), soonest-first, tap→offer, empty-state styled; additive churn feed items; feed diff gate.
Expected files: index.html
Outcome: Shipped: pure `churnEligibleDate`/`churnAnchorDate` + `addMonthsClamped` (month-end clamp via last-day min, local-midnight idiom; Jan31+1mo→Feb28 unit-verified); modal Advanced "Churnability" group (Unknown/Yes/No tri-state, wait-months number, anchor select, notes) wired in readOfferForm; card churn line in renderLifecycleInfo (eligible-now success / eligible-date / needs-anchor muted / not-churnable muted-gated / null silent); `renderOverviewChurnSection` mounted at BOTTOM of renderOverview (full-width card, Eligible-now + Upcoming≤90d buckets, soonest-first, months chip, tap→modal via open-action-target, hides when no churnable offers); feed kind `churn-eligible` id yv-offer-<id>-churn emitted from DEDICATED all-offers loop (closed/terminal offers = natural churn case, main committed-loop would skip) within window [≤60d ahead, ≤180d past] (named consts). Gates: node --check ✓, 17 unit tests ✓, console clean, all scenarios ✓ (close→stamp→compute / reopen→clear round-trip incl.), feed diff: seed byte-identical baseline, only churn-eligible under exercised conditions, envelope frozen, monotonic ✓, 380px zero h-overflow ✓, git scoped ✓.
Files: index.html only
Commit: 00cfc14 (local)
Substituted review (agent a9bf44ad4a8ac4917): 0 P1, 0 P2, 3 P3 → all queued for step-10 sweep: (i) .churn-section grid-column 1/-1 inert (mounts AFTER .overview-grid, block parent — remove prop + fix comment); (ii) churnable=Yes + wait_months=0 → silent everywhere (decide: eligible-immediately or "needs wait period" hint); (iii) churnable offer w/o computable date mounts permanent empty section card (tighten gate to computable-or-needs-date rows). Reviewer refuted: DST/TZ shifts, tri-state null→false flip, feed id collisions, XSS, monotonicity.
Design note (accepted): churn-eligible also appears in Overview "Upcoming actions" LIST via unified builder (isWork:false → does NOT touch Actions-required count, verified) — coherent, kept; screenshot tool blanks on this fixed-overlay app (agent used inspect/snapshot/eval per tool guidance).

### 6. Expiration & freshness surfacing (F4)  [tier: executor (retiered per Codex)] [codex: review-after→substituted adversarial review] [status: done] (amended: also exposes promoted scalars monthly_fee/promo_code/etf_window_days/bonus_post_min/max in modal Advanced)
Intent: "Expires <date> (Nd)" chip on offer cards when expiration set (urgency styling ≤14d, expired styling); NEW sort control for Offers list (none exists — add sort state + UI consistent with existing filter/search controls): options incl. expiring-soon, newest, bonus value; "Updated <date>" stamp (last_edited from step 2) on card. Follow chip/label design tokens exactly.
Expected files: index.html
Outcome: Shipped (+180/-2): `offerExpirationChip` (gated: has date ∧ not closed ∧ subStatus ∉ {earned,denied,didnt-track,archived}; muted >14d / chip-warn ≤14d incl. 0d / chip-danger expired; replaced old always-warning expiry line); `App.filters.offersSort` session-only + `sortOffersList` (stable index-decorated, nulls-last: default|expiring|newest(last_edited)|bonus) applied to grid AND table; `offerUpdatedStamp` (10px --text-tertiary, "Updated today"/date); modal Advanced "Fees & terms" group (monthly_fee$ + waiver text, promo_code, ETF$ + window days, bonus_post min/max one row) w/ null-safe reads; card "$N/mo" muted detail. Verified: chips all states, sort × search/status interplay, 7-field round-trip (money comma-grouped, embedded "$1,000" in waiver text survives), bonus_post form change moves card window live, ETF drives safe-to-close (3650d → 2036 sanity case), feed BYTE-IDENTICAL (signature 1012==1012), 375px wraps clean, node --check ✓, console clean, git scoped. Offers table has NO expiration column → (Nd) suffix skipped per spec branch (noted).
Files: index.html only
Commit: a4be5cd (local)
Substituted review (agent a8fc8d5c2454ae22f): 0 P1, 1 P2, 2 P3 — [P2] chip shows danger "Expired" on met-waiting/approved/on-track (feed correctly suppresses via CONFIRMED statuses; expiration only governs signup) → align chip gate with feed semantics (prospect/applied only); [P3] bonus-sort null→0 makes nulls-last branch dead (comment lies); [P3] "(0d)"/"in 0 days" copy → "Expires today". Reviewer confirmed feed COMPLETELY untouched (removed .offer-dates line was display-only), refuted XSS/phantom-diff/table-keying/stability concerns. All 3 dispatched to implementer (a9c50b50c244ef012).
Fix-up outcome (verified): chip gates on canonical PRE_ACCOUNT_SUB_STATUSES {prospect,applied} (parallel exclusion list deleted; allAligned:true vs feed predicate across all statuses); bonus sort blank→null before Number() (genuine $0 sorts as value, before null bucket); "Expires today"/"act now" same-day case. Feed still byte-identical.

### 7. DoC import v1 — paste-post parser + preview/confirm (F1, Phase 6)  [tier: executor] [codex: review-after→substituted adversarial review] [status: done] (amended: parser also targets churn hints — "once per X years/months" → churnable/churn_wait_months/churn_anchor with confidence)
RESUME NOTE: first attempt cut off by session limit after 54 tool calls (~05:00); working tree survived (parser in index.html, 5 fixtures created, README + testDocParser + gates outstanding); same agent resumed from transcript ~after reset with completion+full-gates instructions.
Outcome: Shipped complete (+898 additive lines): paste entry near docUrl; pure `parseDocPost` w/ ALL 8 quirks (points→note no-$, range→high-end+note, dual-bonus→verify-split note, <del>→struck via detached DOMParser, latest "Update M/D/YY" paragraph wins, fuzzy token-subset labels, max-date, fees use FIRST dollar so "$10, waived with $500"→10) + churn-hint scan (richest eligibility sentence, ±140-char anchor context, default bonus_received, LOW confidence → opt-in); preview panel (confidence dots, verbatim snippets ≤120c, high/med checked low unchecked, overwrite strike-through, Apply N, leftovers list); graceful failure (E_PARSE logError, try/catch, form untouched); DOC_FIELD_MAP 24 entries; apply dispatches input+change → existing write-through/derivation fires; ddRequired only switches type from default; user rows via #f-user-reqs; `_fromImport` de-dupe marker; testDocParser console hook; 5 synthesized fixtures + README expected-value tables. Gates: node --check ✓; 48/48 Node assertions (all quirks + churn) ✓; E2E fixture01 full flow + fixture05 churn/user-rows + overwrite + garbage ✓; XSS probe (<script>/onerror paste) did NOT execute ✓; feed: baseline (now 11 items) restored byte-for-byte after test-offer delete ✓; 375px ✓; git scoped ✓.
Files: index.html (+898); docs/fixtures/doc-samples/{01-05,README.md}
Commit: 83b5cd2 (local). Fix-up verified: DoS 21,939ms→177ms on 80KB pathological line (bounded {0,160} quantifiers everywhere incl. hidden fee-waiver offender + 200KB cap w/ visible truncation notice; 200KB adversarial worst-case ~445ms linear); docDollarToNumber k-suffix (bonus/funding/spend scans); docDaysFrom weeks×7 (posting/hold/DD/deposit windows); de-dupe type+label. 67/67 Node harness, 44/44 in-browser testDocParser, feed baseline restored.
Agent open issues → carried: `_fromImport` inert boolean persists on imported rows (no consumer; hygiene-strip candidate step 10/11); parser never invents deadline_days for spend/transactions (correct — no data source); 6 intentional E_PARSE diagnostics from garbage tests (by design).
Substituted review (agent a2d0b7d3add2a02a1, jsdom-empirical, 43/43 fixture assertions independently reproduced): 0 P1, 2 P2, 1 P3 — [P2] quadratic-regex DoS (posting-window + body scans, unbounded [^.\n]* both sides of \d+; measured 17.7s on 80KB line; no textarea cap) → input cap + bounded quantifiers; [P2] "$1.5k"→1.5 (~1000× small, HIGH confidence, checked by default) → k-suffix multiplier; [P3] "posts within 6-8 weeks" silently dropped → ×7 conversion. XSS defense VERIFIED SOLID empirically (detached DOMParser, textContent-only, escapeHtml at every sink, no javascript:-URL path, card sinks escape imported values); fixtures confirmed synthetic; _fromImport confirmed inert; Apply ordering/no-clobber confirmed. Bonus fix assigned: _fromImport de-dupe key type→type+label. All dispatched to implementer (a915a1aaa3cf68223).
Intent: Phase 6 per `docs/assessments/2026-07-05/report.md` §Phase 6, against the NEW schema: "Paste DoC post" entry in offer create/edit; deterministic glance-list parser (8 corpus quirks: points-not-dollars, ranges, combined dual-bonus totals, `<del>` strikethrough history, "Update M/D/YY" recency reconciliation, fuzzy label matching, …); maps to requirement rows + promoted fields; preview panel w/ per-field confidence + verbatim source snippet + explicit confirm-before-overwrite; graceful fallback to manual; `logError`-gated. CREATE `docs/fixtures/doc-samples/` with 4-6 SYNTHESIZED sample posts covering the quirks (no wholesale DoC copying; cite structure-source URLs in a fixtures README) + use them to verify parsing in preview.
Expected files: index.html, docs/fixtures/doc-samples/*
Outcome:
Files:
Commit:

### 8. DoC import v2 scaffold — gated (F1, Phase 7)  [tier: executor (retiered per Codex)] [codex: review-after→substituted adversarial review] [status: done]
Intent: `cloudflare/doc-import-worker.js` (fetch DoC URL server-side w/ allowlist `doctorofcredit.com`, CORS restricted to the Pages origin, calls Anthropic API — model per claude-api skill current ids — for ~5 low-confidence prose fields, returns JSON; API key ONLY in Worker env) + `cloudflare/README.md` (deploy steps, security notes); client: Settings field "DoC import Worker URL" (empty default), URL-import mode visible ONLY when configured; client-side verbatim-quote tripwire before accepting LLM fields; failure = graceful fallback to v1 paste mode. Zero secrets in repo; zero network calls unless configured. NOT deployed tonight — morning task documented.
Expected files: cloudflare/doc-import-worker.js, cloudflare/README.md, index.html
Outcome: Shipped gated scaffold: Worker (module syntax; POST-only; exact-host allowlist doctorofcredit.com/www; CORS from env.ALLOWED_ORIGIN fail-closed + OPTIONS 204; 10s AbortController + ~1.5MB body cap; entry-content/article readability w/ whole-page fallback; Anthropic call model env||claude-sonnet-5 w/ tripwire system prompt; llm:null on LLM failure so v1 parser proceeds; pure helpers as named exports for unit tests) + README (wrangler/dashboard deploy, env table, security notes, troubleshooting) + client (+238/-5): docWorkerUrl in device-local SYNC_CONFIG_KEY (never synced payload; survives disconnect), Settings field w/ https validation, gated Fetch-from-URL mode feeding the EXISTING parseDocPost→preview pipeline, LLM fields GAP-FILL ONLY (parser wins), client-side tripwire re-verification (defense-in-depth vs hostile Worker), AI badge + LOW/unchecked + "N failed verification" note, graceful degradation to paste. Gates: node --check both files ✓; unconfigured = byte-identical v1 (zero DOM traces) ✓; mock-endpoint E2E (:8899): fetch→16 fields, grounded AI field in w/ badge, fabricated-quote field discarded, apply ✓, killed-mock error path ✓; 47-check Worker unit harness (allowlist evil.com/subdomain/suffix-spoof/http rejected, OPTIONS/CORS, cap, parseLlmJson, groundLlmFields) ✓; git scoped ✓; secret grep 0 ✓. NOT deployed — morning task documented.
Files: cloudflare/doc-import-worker.js (new), cloudflare/README.md (new), index.html
Commit: 6e7d103 (local)
Substituted review (agent a94dee7dee9cc3a8f): 0 P1, 1 P2, 2 P3 — [P2] fetchPage redirect:'follow' w/o final-URL re-validation defeats allowlist (open redirector on DoC host → arbitrary fetch + LLM credit burn; capped P2: body cap survives redirect, CF egress can't hit RFC-1918) → manual redirects, ≤3 hops, re-validate each; [P3] originAllowed true when Origin absent → curl bypasses CORS, unauthenticated credit burn → optional env.WORKER_SECRET + X-YV-Key header (client secret in device-local config, never synced); [P3] README overstates ("can't be SSRF pivot", CORS-gates-all-callers implication) → precise rewording + secret guidance + CF rate-limit pointer. Reviewer verified clean: client XSS (all sinks escaped incl. AI note count coercion), config/export/Gist leak-free, gating complete, key-leak-free error paths, streamed body cap, userinfo/port URL tricks defeated, JSON-400/OPTIONS-403 correct; prompt-injection-via-planted-quote judged documented-accepted-residual (gap-fill + low/unchecked + visible quote + notes-only landing). All 3 dispatched to implementer (a48bc0a29ba50bc64).
Fix-up outcome (verified): manual redirect loop w/ pure `decideRedirect` (≤3 hops, per-hop allowlist, relative Locations resolved, http-downgrade rejected); `secretOk`/`safeEqual` gate BEFORE body parse/fetch/LLM (ordering proven in tests); client "Worker secret" password field (device-local blob, survives disconnect, verified absent from App.state/export; sent as X-YV-Key only when set; saved independent of URL's https gate); README precise. 77/77 worker checks; secret E2E via mock (sent/absent/wrong-secret-403 paths).
Morning task (UPDATED): deploy worker + `wrangler secret put ANTHROPIC_API_KEY` + `WORKER_SECRET` (recommended) + var ALLOWED_ORIGIN=https://cmr2334.github.io; paste URL + secret into Settings; optional CF rate-limit rule.

### 9. Offer template cache — personal mini Deal Radar (F5)  [tier: executor] [codex: review-after→substituted adversarial review] [status: in-progress]
Intent: "Save as template" on any offer — strips ALL personal instance data (dates incl. open/received/closed, entityUsed, emailUsed, notes, churn_notes, done-state, any account identifiers; keep only offer terms); template library UI (browse/search by bank/type, chips: bonus + key requirement; one-tap "Track this" → fresh pre-filled offer); persisted via existing localStorage+Gist channel under its own key; DoC import confirm offers save-as-template. RSS auto-ingest OUT of scope (documented as future option).
Expected files: index.html
Outcome: Shipped: pure `offerToTemplate`/`templateToOffer` w/ WHITELIST strip (`TEMPLATE_TERMS_KEYS`, 21 keys travel: terms + color cosmetic + docUrl provenance + expiration; future personal fields can't leak by construction; dd/debit copied as CONFIG only, user rows stripped of done/done_date/notes/_fromImport, derived rows dropped); `state.templates=[]` root key + tolerant guard inside migrateOffersToSchemaV2 (10 call sites = boot + 8 adoptions + JSON import) → rides Gist/export naturally, old builds tolerate (adopt paths are wholesale, no key allowlist — verified); UI: modal "Save as template" (reads CURRENT form) + card action-row "Template" button (reads STORED offer) w/ case-insensitive bank+offer dedupe confirm-replace; collapsed searchable "Start from a template (N)" picker in new-offer flow (Use → showOfferModal(null, seed) — form renders template wholesale, robust vs form drift; delete w/ confirm; affordance hidden at 0); DoC post-Apply "Also save as template" checkbox (default unchecked, stateless DOM read, silent upsert to avoid dialog stacking). Gates: node --check ✓; 135/135 strip assertions (17 personal ABSENT, 20 terms PRESERVED, round-trip stable, input unmutated) ✓; feed no-op proven 2× (harness byte-identical 0 vs 3 templates; in-browser 11-item identical; no tpl_ ids in feed) ✓; full E2E ✓; 380px zero h-scroll (@media 420px one-column) ✓; git scoped ✓.
Files: index.html only
Commit: (pending substituted adversarial review)
Agent notes: two save entry points intentionally differ (modal = form-context w/ unsaved edits; card = stored offer); #tpl-search binds in modal's setTimeout(0) idiom (unreachable race).

### 10. Aesthetic coherence + sharpening pass  [tier: executor] [codex: review-after] [status: pending]
Intent: Sweep ALL new UI from steps 3-9 for design-language consistency (0.02em uppercase-label letterspacing, field-box rhythm per R61, `.btn-grid` patterns per R60, display-format helpers for every date/money render); sharpen overall aesthetics where clearly appropriate (restraint: consistency > novelty); verify mobile ~380px + desktop; no horizontal scroll; console clean.
Expected files: index.html
Outcome:
Files:
Commit:

### 11. Full verification + docs + wrap + single push  [tier: executor] [codex: review-after] [status: pending]
Intent: End-to-end smoke in preview: clean console; migration re-test from pre-run-shaped seed (incl. idempotency + backup restore path); exercise every feature (requirement rows + computed dates + done toggles, lifecycle + windows + safe-to-close, churn overview section, expiring chips + new sort, DoC v1 paste on all fixtures, v2 gating stays hidden unconfigured, template save/instantiate with personal-data-strip verification); fixed-seed feed diff = additive-only vs baseline; update `docs/SHORTCUT_BUILD_GUIDE.md` (new feed kinds), HANDOFF.md round entry (incl. APP_VERSION correction note), CHANGELOG.md, README.md (DoC v1 out of Roadmap; v2 pending Worker deploy); bump APP_VERSION → `2026.07.08a`; final `git status --short` audit (R62-R64 untouched); THEN single `git push` of all step commits; report.
Expected files: index.html, HANDOFF.md, CHANGELOG.md, README.md, docs/SHORTCUT_BUILD_GUIDE.md
Outcome:
Files:
Commit:
