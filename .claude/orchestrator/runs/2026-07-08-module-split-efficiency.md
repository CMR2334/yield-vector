---
run: 2026-07-08-module-split-efficiency
task: "the overnight work to be done with significant calling upon Codex to offload token burden is to both plan the migration into multiple files, and in the process audit the entire codebase and look for places to be more efficient, but with a relentless emphasis/focus on not compromising quality, features, formatting, etc. from it's current state via erroneous deletion. [Owner approved sequence; proceed thoughtfully; auto-resume on usage-window reset without manual nudge.]"
status: in-progress
created: 2026-07-08T01:40:00-05:00
config_snapshot: { planner: fable, executor: opus, worker: sonnet, codex: { planner: review-before-approval, executor: review-after, worker: review-after } }
plan_approved: pre-authorized (owner, verbatim above; codex plan critique still runs and folds in before execution)
current_step: 3 (P0 context phase IN FLIGHT; split-contract commit + pre-modules tag pushed; baseline green 09:14 — syntax OK, fidelity 67/67, pins 20/20; owner morning task still open: npx wrangler deploy for the Worker title upgrade)
---
## Context
Split Yield Vector's single-file `index.html` (~14.5k lines, hit the agreed 15k structural trigger) into native ES modules — same repo, same GitHub Pages URL, zero build step — and run a full-codebase efficiency audit/consolidation in the process. OWNER'S PRIME DIRECTIVE: nothing lost — no feature, behavior, formatting, or aesthetic regression; "erroneous deletion" is the named fear. Codex does the heavy reading/analysis (token offload, owner-directed). Starts ONLY after the action-tracking release (v2026.07.08e) ships. Safety net: full existing verification stack — testDocParser 63/63 + 13 regression pins, corpus score ≥84.9% via docs/fixtures/doc-corpus/harness, feed byte-identity on fixed seed, preview E2E, 380px pass — ALL must stay green at every phase boundary.

## FRESH-SESSION HANDOFF (written 2026-07-08 ~07:00, owner-requested; supersedes nothing — this section is the entry point)
**[STAMP 07-08 09:2x — §6 EXECUTED: (a) marker grep = 8 ✓; (d) baseline green 09:14 (syntax OK, fidelity 67/67, pins 20/20) ✓; (b) split-contract commit ✓; (c) tag `checkpoint-2026-07-08-pre-modules` pushed ✓; (e) step-3 P0 dispatched to executor. Live state = frontmatter + Steps below; this section is historical context.]**
**1. Mission/scope:** Split `index.html` (~15,000 lines / 775KB single inline script) into 17 native ES modules (same repo/URL/hosting, zero build step) + work a ranked efficiency-consolidation backlog. PRIME DIRECTIVE: zero feature/behavior/formatting loss; "erroneous deletion" is the owner's named fear. Codex offload for heavy reading (outage mode = 5h usage-window exhaustion, NOT credits [owner-confirmed 07-08 09:14, reset ≈09:52]; during a window outage substitute independent adversarial Opus). Follow-on after this run: the planner-optimizer (see "Follow-on" section below; churn-hybrid design agreed).
**2. Exact phase:** Steps 1 AND 2 done — the design-doc amendment COMPLETED at ~07:07 (522-line contract; all 7 gate findings + 2 adjacent notes folded; phase table now P0 context → P1 leaves ×5 → P2 atomic SCC ×12 (one squashed move-only commit) → P3 shell; setter sweep of all 5 top-level let/var confirms EXACTLY ONE sanctioned change-class edit: `seedManifestHwm(v)`; harness×3 conversion at P1-end w/ extraction-identity gate; full SW spec w/ APP_VERSION import-map versioning + api.github.com passthrough + controllerchange→reload). ZERO split code exists: no js/ dir, no SW, no code moved. Step 3 NOT started — awaiting owner nudge (run parked by owner instruction).
**3. Complete:** v2026.07.08e committed+pushed = `447c57f` (action tracking + reverse channel + owner UX batch + parser anchor/waiver fixes); parser battery green at last run: fidelity **67/67**, regression pins **20/20** (corpus 84.9% baseline stands; full re-score needs jsdom + re-hydrated posts); step-1 Codex audits complete (4 analyses, spot-verified A/A/A/A) → 17-module map in `docs/assessments/2026-07-08-module-split-design.md` (26.9KB, **UNTRACKED on disk, not yet committed**); offline decision made: **Path B — minimal network-first service worker** (constraint-driven: offline-capable is a named app constraint); plan gate found **2 P1 bootability issues** (cross-module `_manifestHwm` rebind = SyntaxError; DOM/state SCC split across phases = no valid incremental order) + 3 P2 + 2 P3 — full dispositions in step 2 below; heartbeat cron `428910a9` CANCELLED via CronDelete at owner request.
**4. NOT complete:** design-doc amendment (VERIFY FIRST — see 6), design-doc commit, pre-split tag, all of step 3 (module extraction), SW, harness conversion to module imports, step 4 consolidations, docs/ARCHITECTURE.md, APP_VERSION 2026.07.09a, final push. Optimizer: not started. Owner morning tasks outstanding: `npx wrangler deploy` from cloudflare/ (Worker title upgrade), optionally add Codex credits.
**5. Amended contract (binding for step 3):** (a) extraction order = TRUE LEAVES first (a module is extractable only when everything it CALLS is already extracted or co-phased) → then the ENTIRE 10-module DOM/state SCC (app-state, sync-pwa, dd-widgets, projection-optimizer, reminders, render-shell, render-main, doc-import-templates, modals-forms, events-actions-data) in ONE ATOMIC phase → then shell/bootstrap; (b) MOVE-ONLY commits, byte-preserving, with the ONLY sanctioned CHANGE-class edits being declared setters for cross-module let/var reassignments (`seedManifestHwm(v)` in reminders.js for App.init@~5461; plus any others the amendment sweep declares — nothing silent); (c) ALL THREE harness scripts (docs/fixtures/doc-corpus/harness/{parser-loader,regressions-check,p2b-segmentation-pin}.js) convert to ES-module imports at the FIRST phase that moves their symbols, gated by extraction-identity (old-extraction vs new-import on identical code must score identical); (d) SW spec: module URLs versioned `?v=APP_VERSION` + precache named by APP_VERSION + fetch handler EARLY-RETURNS passthrough for cross-origin and api.github.com (gist NEVER cached) + controllerchange→reload; (e) render-string consolidations gated by objective DOM/innerText snapshot diff on the fixed seed, not visual-only; (f) deletion ratchet: remove only on Codex-flag AND independent Claude zero-reference confirmation (incl. dynamic dispatch: data-action strings, DOC_FIELD_MAP keys, JS-built CSS classes) AND all gates green; (g) full battery per phase: JS syntax check, fidelity 67/67, pins 20/20, corpus ≥84.9 (or documented alignment reasoning when bodies unavailable), feed byte-identity on fixed seed, preview E2E (+offline-reload once SW exists), 380px.
**6. FIRST ACTION for a fresh session (amendment CONFIRMED complete — no verification branch needed):** (a) sanity-grep `seedManifestHwm` in the design doc (expect >0); (b) commit the design doc + this checkpoint as the "split contract" commit; (c) create+push tag `checkpoint-2026-07-08-pre-modules`; (d) run the §9 baseline verification commands; (e) begin step-3 **P0** exactly per the design doc's §6 phase table (P0 = tag + script→module + bootstrap import block + side-effect consolidation, full battery before P1). Do not write any module code before (b)-(d) are done.
**7. Read first:** this file (whole); `docs/assessments/2026-07-08-module-split-design.md` (the contract); HANDOFF.md Current state + top 2 entries; `docs/fixtures/doc-corpus/verification-log.md` (how the numbers are reproduced); repo CLAUDE.md (push protocol pointers).
**8. AVOID reading unless necessary:** `index.html` wholesale (15k lines — the design doc §2 has line-range provenance per module; grep targeted ranges only); `~/.claude/projects/-Users-collinrekowski/807abe14-*/subagents/agent-*.jsonl` (multi-MB transcripts); `docs/fixtures/doc-corpus/labels/` in bulk; any corpus post bodies (not in repo). AGENTS.md is OWNER-DIRTY (uncommitted R62-R64) — never edit; CLAUDE.md/.claude/settings.json/.codex likewise.
**9. Verification commands BEFORE any edit (expect-green baseline):**
```
cd "/Users/collinrekowski/Automation/Yield Vector"
git status --short   # expect ONLY: M .claude/settings.json, D .codex/hooks.json, M AGENTS.md, M CLAUDE.md, M <this runs file>, ?? docs/assessments/2026-07-08-module-split-design.md
python3 -c "import re;h=open('index.html').read();open('/tmp/yv-check.js','w').write('\n;\n'.join(re.findall(r'<script>(.*?)</script>',h,re.S)))" && node --check /tmp/yv-check.js
cd docs/fixtures/doc-corpus/harness && npm i --no-save jsdom && node fidelity-check.js   # 67/67
node regressions-check.js   # 20/20
```
Feed byte-identity + preview E2E run per phase via the preview server (`.claude/launch.json` → `yield-vector-static`, port 4173).
**10. Git state at handoff:** HEAD = `447c57f` (pushed; == origin/main). Dirty: the 4 owner files (`M .claude/settings.json`, `D .codex/hooks.json`, `M AGENTS.md`, `M CLAUDE.md` — NEVER commit/revert these) + this runs file (`M`) + the untracked design doc. `git diff --stat` ≈ 5 files, +17/−80 (owner's tooling trims + checkpoint bookkeeping).
**11. Audit outputs & transcripts:** the four Codex audit reports live in the SESSION SCRATCHPAD (`/private/tmp/claude-501/-Users-collinrekowski/807abe14-*/scratchpad/audit-{boundaries,dead,dupes,css}.md`) — EPHEMERAL, may be gone in a fresh session; their durable synthesis IS the design doc (line-range provenance included), which is sufficient. Subagent transcripts persist under `~/.claude/projects/-Users-collinrekowski/807abe14-*/subagents/` — reference-only, avoid bulk reads.
**12. NO HEARTBEAT/AUTO-RESUME EXISTS:** cron `428910a9` was cancelled by the owner. After any usage-window cutoff or session restart, continuation requires a manual nudge (e.g. `/orchestrate resume 2026-07-08-module-split-efficiency` or just "continue the module split run").

## Key decisions
- Overnight autonomy: pre-authorized by owner incl. auto-resume after usage-window resets (hourly cron `yv-overnight-resume` checks for a stalled in-progress run and resumes; planner deletes the cron at run completion). UPDATE ~07:3x: cron 428910a9 CANCELLED early at user request — auto-resume after a usage cutoff is no longer armed. While the session is healthy, agent-completion notifications drive the run normally; if a cutoff strikes mid-step, resumption needs a manual nudge ("continue" or /orchestrate resume — the checkpoint makes it a one-liner). Step 5's delete-cron task is now moot.
- Codex offload: audit analyses run via `codex exec --sandbox read-only` (boundaries/dead-code/duplicates/CSS) — findings are INPUTS, never applied without Claude executor verification + gates.
- Deletion ratchet (anti-erroneous-deletion): code may be removed ONLY when (1) Codex flags it dead AND (2) a Claude executor independently confirms zero references (call-graph + dynamic dispatch check: onclick strings, data-action names, DOC_FIELD_MAP keys, CSS selectors vs DOM classes) AND (3) all gates stay green after removal. Anything failing any leg stays. Every removal listed in the checkpoint with its double-confirmation evidence.
- MOVE-ONLY vs CHANGE commits strictly separated: split phases relocate code byte-preserving (only export/import statements added); consolidation commits come after the split is fully green, one consolidation class per commit, each independently revertable.
- Rollback: tag `checkpoint-2026-07-08-pre-modules` at the pre-split HEAD, pushed immediately. Push policy: local commits per phase; push at each fully-green phase boundary (split-complete, then per consolidation batch) — NOT per intermediate move.
- PWA/service-worker: cache manifest must enumerate new module files; preview verification includes offline-reload check + a hard-refresh cache-bust story (bump cache version per existing convention if one exists — investigate).
- AGENTS.md is still dirty with owner's uncommitted R62-R64 edits — MUST NOT be touched; the new architecture map goes in HANDOFF + a new docs/ARCHITECTURE.md (flag to owner: fold into AGENTS.md after he commits his pending changes).
- The corpus harness's parser-loader extracts from index.html — the split WILL break its extraction path; step 3 must update the loader (repo `docs/fixtures/doc-corpus/harness/parser-loader.js`) to import the new module directly (this is an IMPROVEMENT the split enables) and re-verify 84.9% unchanged.

## Prerequisite (08e release, before this run starts)
The in-flight action-tracking step's release MUST also include SIX owner items (dispatch to agent a0b0591cd4ec6b42f before its commit; if that agent is dead, fresh executor): (1) chart legends back to STACKED but tighter — 2-column grid: overview chart 2×3, timeline 2×2 (owner correction of the flex-wrap change); (2) tier-picker rows gain an annualized-return chip: rate = tier.bonus / tier.threshold_min, annualized ×(365 / lockDays) where lockDays = tier.hold_days || offer-level daysFundsMustRemain — when NO lock duration known, plain ROI% labeled "(no hold data)"; chip "≈N%/yr", muted; verify BofA contrast (small tiers ≫ $250k rung). (3) Tier radio bubbles vertically CENTERED against the row body (owner; was the 4b noted nit). (4) Waiver-truncation bug (owner's live BofA URL import): fee_waiver_condition captured "…otherwise you must:" and DROPPED the following bullet list — when a captured condition ends with a colon and bullets follow in the same segment, append the bullets joined as "; or " clauses (cap ~240 chars, ellipsis beyond); add fixture coverage (colon+bullets waiver pattern). (5) Capitalize-first-letter rule: text values applied into form boxes via _docSetInput get capFirst — EXCLUDING promo_code (case-sensitive!) and URL fields; verify "smaller companies…"→"Smaller companies…". (6) Worker-path bank/offer-name gap (owner: URL import filled neither): page TITLE lives outside entry-content so the Worker never returns it → parser title heuristic finds nothing. Fix BOTH sides: cloudflare/doc-import-worker.js additionally returns {title: <page <title>/og:title/h1>} (backward-compat field); client prepends title as first line of the text fed to parseDocPost when bank/offerName would otherwise be absent, with a slug-derived fallback (URL path → words, LOW confidence) when title also absent. NOTE FOR OWNER: Worker fix requires his one-command redeploy (npx wrangler deploy) — morning task, client is backward-compatible either way. (7, DANGEROUS-class from owner's live BofA import) Hold anchor bug: parser wrote daysFundsMustRemain=60 anchored "(from funded date)" for "deposit within 30 days of account opening and maintain the balance from days 31 through day 90" — correct is 90 anchored FROM ACCOUNT OPENING (lockStartsFrom opening/'signup' enum). 60-from-funded under-holds by up to 29 days → bonus-clawback risk (app would say withdrawable day 61 when the real requirement is day 90). Pattern family "days X through (day) Y" in opening-anchored context → count=Y AND lockStartsFrom=opening; "maintain for N days after funding" → funded anchor, count=N. Locate + subordinate whatever path produced the 60. Regression pins BOTH anchor directions; fixture 06 expectations → 90/opening. ALSO: verify whether any import path writes plannedSignupDate (owner saw 7-15-2026 appear) — if import fills it, remove (personal planning data never imported); if it's a form default, report-not-change. Addendum delivered live to agent a0b0591cd4ec6b42f mid-round. (8, owner 03:5x) DoC "↗" card link moves BELOW the "Updated today" stamp text in the card foot (stacked, not side-by-side). RESUME NOTE: final-round agent died at session limit 02:08 mid-item-4 (_docSlugTitle); worker title changes landed, client helpers partial; resumed ~03:51 after window reset via heartbeat protocol w/ verify-then-complete instructions.

## Follow-on (conditional, owner-directed)
If this run completes fully green + pushed and the usage window allows: planner drafts the OPTIMIZER run checkpoint (constraint-based sequencing: slide sign-up dates, simulate capital curve incl. deposits/holds/withdrawals, hard min-cash-buffer floor, maximize return; churn-ready offers auto-considered as candidates BADGED "stored value from <date> — unverified" with prompt-gated Worker re-check to firm them up — agreed hybrid design), runs the codex plan critique, folds findings, and BEGINS execution (design/scaffold steps first) under the same overnight autonomy + gates. The optimizer builds on the NEW module structure.

## Steps
### 1. Codex audit fan-out + synthesis → design doc  [tier: executor, codex-heavy] [status: done]
Outcome: Design doc docs/assessments/2026-07-08-module-split-design.md (26.9KB): 17 modules + inline bootstrap (leaves-first load order; render-main-views ~1,980 + modals-forms ~1,730 lines w/ optional sub-splits deferred); dead code = 6 HIGH-only candidates (conservative, good), 12 dupe clusters (5 EASY/5 MODERATE/2 RISKY; schema-v2 §B exclusion honored), CSS ~10 orphans + 7 dynamic-do-not-remove, keep-CSS-inline; dispatch layer needs NO window exports (111 data-action via lexical switch; App/Sync/YieldVector exposed for console/harness only); NO service worker exists (runtime-generated manifest — prior assumption corrected); 5 reference cycles are call-time → MOVE-ONLY relies on ES live bindings, cycle refactors deferred to CHANGE-class backlog; harness parser-loader switches to module import at P3. Audits graded A/A/A/A (~17 sampled claims, 0 wrong; Codex caught 2 inline onchange handlers the Claude survey missed). OFFLOAD REAL: Codex ~5 full-file ingestions (~1-2M input tokens on OpenAI credits) vs Claude <1 file-equivalent. Failure lesson recorded: backgrounded codex jobs are reaped when the agent's turn ends → synchronous/waiter-loop pattern required (heartbeat caught the stall at 05:51, resumed 04:54-stalled agent).
Files: docs/assessments/2026-07-08-module-split-design.md (new); scratchpad audit-{boundaries,dead,dupes,css}.md
Commit: (folds into step-3 phase-1 commit)
PLANNER DECISION (offline): Path B — minimal ~40-line network-first service worker. Rationale: offline-capable is a NAMED app constraint; single-file offline came free via one cached document; N-file plain-HTTP-cache offline is not guaranteed (Pages max-age=600 + revalidation-on-reload); a tiny SW is in-browser code (not infra) and preserves the constraint. Step-2 critique pressure-tests this.

### 2. Plan gate on the design doc  [status: done — amended contract delivered ~07:07; run parked by owner pre-step-3]
CODEX OUTAGE #2 (~06:4x): workspace out of credits again — consumed by the four step-1 full-file audits (the intended offload; reading is banked in the audit files + design doc). Plan-gate critique substituted per established pattern: independent adversarial Opus review of the design doc w/ the same brief (ES-module pitfalls incl. live-binding/let-reassignment/TDZ/side-effect ordering, SW/Pages/iOS pitfalls, phase-order + gate gaps, calibration/feed risk in the consolidation backlog). RESOLVED 2026-07-08 09:14 (owner): NOT a credits problem — the Codex 5h usage window was at 0% (drained by the step-1 offload, as intended); setup probe fully green (CLI 0.135.0, ChatGPT auth verified, shared runtime). Window reset ≈09:52; review-after on split phases routes through Codex again from P0 onward, adversarial-Opus substitution only during future window outages.
Gate findings (2 P1, 3 P2, 2 P3 — doc's factual claims all verified accurate; problems structural): [P1] `_manifestHwm` let reassigned cross-module (App.init @5461 → binding declared @7453) = SyntaxError under ES imports → sanctioned setter (seedManifestHwm) + sweep for other cross-module let/var reassignments, each explicitly declared; [P1] DOM/state SCC spans planned P4-P8 → NO valid incremental order (external modules can't import from inline script) → restructure: true leaves first (leaf = everything it CALLS already extracted), then the ENTIRE 10-module SCC in ONE ATOMIC phase, then shell; [P2] all THREE harness scripts brace-match-extract and break at P1 (not P3) → convert to module imports at first-symbol-move + extraction-identity gate (old-vs-new extraction on same code must score identical); [P2] SW mixed-version window → ?v=APP_VERSION on module URLs + version-named precache + deploy-transition spec; [P2] SW must passthrough cross-origin/api.github.com (gist NEVER cached); [P3] objective DOM/innerText snapshot-diff gate for render-string consolidations; [P3] controllerchange→reload/update-nudge for iOS. All dispatched to design author (a13ac333bea054cee) as contract amendments. Adjacent notes folded: typeof-Sync guard becomes always-true (Tier-3 cleanup note), _isoFromYMD export surface confirmed at P1.
Intent: Critique the design doc for structural flaws (ES-module pitfalls incl. live-binding/let-reassignment/TDZ, SW/Pages/iOS, phase order, gate gaps); planner folds findings into the doc as binding contract amendments; pre-authorized proceed. (The step-1 Intent text that sat here was a paste artifact; the duplicate pending step-2 block was removed — this block is the single step-2 record.)
Expected files: docs/assessments/2026-07-08-module-split-design.md (amendments)
Outcome: Amended contract delivered ~07:07 (522-line doc): all 7 gate findings + 2 adjacent notes folded; phase table P0 context → P1 leaves ×5 → P2 atomic SCC ×12 (one squashed move-only commit) → P3 shell; setter sweep of all 5 top-level let/var confirms EXACTLY ONE sanctioned CHANGE-class edit (`seedManifestHwm(v)`); harness ×3 convert at P1-end w/ extraction-identity gate; full SW spec (APP_VERSION-versioned module URLs, api.github.com passthrough, controllerchange→reload). Fresh-session verification 09:0x–09:14: marker grep = 8 ✓; baseline green (JS syntax OK, fidelity 67/67, pins 20/20).
Files: docs/assessments/2026-07-08-module-split-design.md (amended; committed as the split-contract commit)
Commit: (backfilled after the contract commit)

### 3. Mechanical split, phased MOVE-ONLY  [tier: executor ×phases, sequential] [status: in-progress]
P0 IN FLIGHT (dispatched 09:2x, executor = Opus 4.8 subagent): context phase per design doc §6 P0 row — no domain code moves; tag already created by planner. Gate numbers per amended contract §5(g): fidelity 67/67 + pins 20/20 (the Intent line's "63/63 + 13 pins" is stale). Local commits only, NO push. Codex review-after at the phase boundary (window resets ≈09:52).
Intent: Tag + push `checkpoint-2026-07-08-pre-modules`. Then per design-doc order, phase-by-phase: extract domain(s) to /js modules (byte-preserving moves + export/import wiring only), slim index.html progressively, update service-worker/PWA cache list, update corpus-harness parser-loader to module imports when the parser moves. FULL GATE BATTERY at every phase boundary (63/63 + 13 pins + corpus ≥84.9 + feed byte-identity + preview E2E incl. offline reload + 380px). A phase that can't get green gets reverted, not forced. Local commit per phase; push at split-complete green.
Expected files: index.html, js/*.js, docs/fixtures/doc-corpus/harness/{parser-loader,regressions-check,p2b-segmentation-pin}.js (P1-end), sw.js + cache manifest (P3 shell)
Outcome:
Files:
Commit:

### 4. Efficiency consolidations (ratcheted)  [tier: executor; codex review-after each batch] [status: pending]
Intent: Work the ranked backlog from step 1: one consolidation class per commit (e.g. formatting-helper unification, CSS dedup, confirmed-dead removals) under the deletion ratchet (Codex + Claude double-confirmation + gates). Codex `codex-companion.mjs review --scope working-tree` after each batch. Stop-loss: any gate regression → revert that batch, log, continue with next class. Push per green batch.
Expected files: js/*.js, index.html (shell), possibly CSS
Outcome:
Files:
Commit:

### 5. Full verification + docs + wrap  [tier: executor + planner] [status: pending]
Intent: End-to-end battery on the final tree (all gates + full feature smoke incl. DoC import E2E on fixtures, tier picker, churn flows, templates, action tracking, sync-config surfaces); docs/ARCHITECTURE.md (module map + load order + conventions for future sessions); HANDOFF R71+ + CHANGELOG + APP_VERSION → 2026.07.09a; line-count + size before/after report incl. every deletion's evidence table; final push; planner deletes cron `yv-overnight-resume`; morning report.
Expected files: index.html, js/*, docs/ARCHITECTURE.md, HANDOFF.md, CHANGELOG.md
Outcome:
Files:
Commit:
